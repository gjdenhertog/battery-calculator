---
phase: 05-visualizations-polish-transparent-assumptions-ui
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/ui/transparency-panel.ts
  - src/ui/tooltips.ts
  - tests/transparency-panel.test.ts
  - tests/tooltips.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03]
must_haves:
  truths:
    - "A collapsible 'Hoe is dit berekend?' panel lists the five simulator assumptions in plain Dutch (round-trip loss, DoD, power clamping, saldering simplification, period framing)"
    - "The saldering assumption restates the full caveat (2026 64% cap, terugleverkosten regardless, 50% floor through 2030) — D-08 depth view"
    - "A 'Waarom geen euro's?' subsection states only why kWh-only is honest — no v2 promise (D-09)"
    - "Technical terms are dotted-underline tooltips that work on desktop hover, keyboard focus, and mobile tap-toggle (D-11)"
  artifacts:
    - path: "src/ui/transparency-panel.ts"
      provides: "Static DOM builder for the collapsible assumptions panel (UX-01, UX-02)"
      exports: ["renderTransparencyPanel"]
      min_lines: 60
    - path: "src/ui/tooltips.ts"
      provides: "Tap-toggle + Escape tooltip wiring (UX-03 / D-11)"
      exports: ["initTooltips"]
      min_lines: 25
    - path: "tests/transparency-panel.test.ts"
      provides: "jsdom DOM-contract for panel + no-euros + saldering caveat"
    - path: "tests/tooltips.test.ts"
      provides: "jsdom term-tooltip attribute + tap-toggle assertions"
  key_links:
    - from: "src/ui/transparency-panel.ts"
      to: ".transparency-panel CSS + native <details>"
      via: "document.createElement details/summary"
      pattern: "createElement\\('details'\\)"
    - from: "src/ui/tooltips.ts"
      to: ".term-tooltip--open CSS class"
      via: "touchstart classList toggle"
      pattern: "term-tooltip--open"
---

<objective>
Build the collapsible transparency panel (UX-01/UX-02) and the technical-term tooltip wiring (UX-03), both consuming the Plan 02 CSS.

Purpose: This is the honesty/depth surface. The panel restates the simulator's assumptions in plain Dutch — including the FULL saldering caveat (D-08, panel = depth vs the table disclaimer = glance) — and a "Waarom geen euro's?" subsection that is a boundary statement, NOT a v2 teaser (D-09). Tooltips make the seven technical terms discoverable on desktop hover, keyboard focus, and mobile tap (D-11), without native title= and without inline styles. No file overlap with the charts (Plan 03), so this runs in the same wave.
Output: src/ui/transparency-panel.ts, src/ui/tooltips.ts, and their jsdom tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md

<interfaces>
<!-- The panel is a PURE DOM builder (no signals), analog: src/ui/readout.ts renderReadout(): HTMLElement.
     The tooltip module is event-wiring (no signals), analog: src/ui/drop-zone.ts listener + classList toggle.
     All copy is locked in UI-SPEC Copywriting Contract (Transparency Panel Copy + tooltip term table) — use it verbatim. -->
renderTransparencyPanel(): HTMLElement   // returns a <section class="transparency-panel"> subtree
initTooltips(): void                     // attaches document-level touchstart + keydown listeners once
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: transparency-panel.ts builder (UX-01, UX-02)</name>
  <files>src/ui/transparency-panel.ts, tests/transparency-panel.test.ts</files>
  <read_first>
    - src/ui/readout.ts (THE analog: pure DOM builder returning HTMLElement, static copy via textContent, named export)
    - src/ui/comparison-table.ts lines ~28-38 (module-level static copy constants pattern)
    - tests/readout.test.ts (jsdom DOM-contract assertion style)
    - src/styles/transparency-panel.css (Plan 02 — the classes this builder targets: .transparency-panel, .transparency-panel__details/__summary/__body, .assumptions-list, .no-euros-section/__heading/__body)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 3 markup; Copywriting Contract → Transparency Panel Copy: the 5 assumptions + no-euros heading/body VERBATIM)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (transparency-panel.ts section)
  </read_first>
  <action>Create src/ui/transparency-panel.ts exporting `renderTransparencyPanel(): HTMLElement`, modeled on readout.ts. Build a <section class="transparency-panel" aria-label="Berekeningsdetails"> containing a native <details class="transparency-panel__details"> with <summary class="transparency-panel__summary">"Hoe is dit berekend?" (native open/close — zero JS for toggle, D-07). Inside a .transparency-panel__body put a <ul class="assumptions-list"> with five <li> items, each .textContent set to the VERBATIM Dutch copy from UI-SPEC "Transparency Panel Copy": Assumption 1 (round-trip rendement, √ each way), Assumption 2 (diepteontlading/DoD), Assumption 3 (laad-/ontlaadvermogen per tijdstap), Assumption 4 (salderingsvereenvoudiging — this li MUST contain the full caveat per D-08: the 2026 64% cap and the post-2027 situation as written in the UI-SPEC copy), Assumption 5 (periode — geen extrapolatie). After the list add a .no-euros-section with an <h3 class="no-euros-section__heading">"Waarom geen euro's?" and a <p class="no-euros-section__body"> set to the VERBATIM no-euros body copy — which explains ONLY why kWh-only is honest and makes NO v2 promise (D-09). Store the long copy strings as module-level const declarations (comparison-table.ts pattern) and assign via .textContent only — never innerHTML. The panel is collapsed by default (no `open` attribute). Write tests/transparency-panel.test.ts (// @vitest-environment jsdom), readout.test.ts style: assert returned element is a <section> with the transparency-panel class and aria-label; assert a <details> exists and is initially closed (no open attribute); assert the <summary> textContent contains "Hoe is dit berekend?" (UX-01); assert exactly five <li> in .assumptions-list; assert one li mentions the saldering caveat (contains "2027" and "terugleverkosten" or the cap figure — D-08); assert a heading containing "Waarom geen euro's?" exists (UX-02); assert the no-euros body does NOT contain a v2 promise (e.g. does not contain "v2 zal" / "binnenkort" / "komt eraan" — D-09 negative assertion).</action>
  <verify>
    <automated>npm test -- transparency-panel --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- transparency-panel` green: section/aria, closed details, summary text (UX-01), five assumptions, saldering caveat present (D-08), "Waarom geen euro's?" heading (UX-02), no-v2-promise negative assertion (D-09).
    - The five assumption strings match the UI-SPEC Copywriting Contract verbatim (round-trip, DoD, power-clamp, saldering, period).
    - The builder uses textContent for all copy (grep absent for `.innerHTML =` on copy nodes).
    - The panel renders collapsed by default (the <details> has no `open` attribute).
  </acceptance_criteria>
  <done>Transparency panel builds the collapsible Dutch assumptions list with full saldering depth and an honest no-euros boundary statement; jsdom tests green.</done>
</task>

<task type="auto">
  <name>Task 2: tooltips.ts tap-toggle wiring + term-tooltip contract (UX-03)</name>
  <files>src/ui/tooltips.ts, tests/tooltips.test.ts</files>
  <read_first>
    - src/ui/drop-zone.ts lines ~38-41 and ~207-218 (event listener wiring + classList-only state machine, no inline style)
    - src/styles/tooltips.css (Plan 02 — .term-tooltip, .term-tooltip--open::after the JS toggles)
    - tests/comparison-table.test.ts (jsdom attribute + class-toggle assertion style)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 4 markup + interaction; Tooltip Interactions table; the 7-term data-tooltip table)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md (D-10 teruglevering canonical, D-11 tooltip mechanism)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (Pitfall 8 attr(data-tooltip) XSS note)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (tooltips.ts section)
  </read_first>
  <action>Create src/ui/tooltips.ts exporting `initTooltips(): void` (drop-zone.ts listener pattern, no signals). Attach a single document-level `touchstart` listener (passive) that: finds `(e.target as Element).closest('.term-tooltip')`; removes `.term-tooltip--open` from every other open .term-tooltip; toggles `.term-tooltip--open` on the tapped element (and removes from all when the tap is outside any term). Attach a `keydown` listener that on `Escape` removes `.term-tooltip--open` from all and blurs the focused element. Use classList add/remove ONLY — no element.style writes, no inline handlers (CSP). Display itself is CSS (Plan 02 :hover/:focus-visible/--open). Tooltip markup (the .term-tooltip spans with tabindex="0" + data-tooltip) is authored where terms appear in copy; the seven term/data-tooltip pairs are the locked author-defined strings from UI-SPEC — data-tooltip is ALWAYS author copy, NEVER interpolated from user CSV data (Pitfall 8). The executor should also provide a small helper or document the exact span shape so Plan 05 (or the copy authors) can wrap terms: a span with class "term-tooltip", tabindex="0", and a data-tooltip attribute carrying one of the seven verbatim glossary strings; "teruglevering" is the canonical surplus term (D-10). Write tests/tooltips.test.ts (// @vitest-environment jsdom): create a container with two `.term-tooltip` spans (tabindex="0", data-tooltip set); call initTooltips(); dispatch a touchstart whose target is span A → assert A gets .term-tooltip--open and B does not; dispatch touchstart on span B → assert B open, A closed (only one open at a time); dispatch an Escape keydown → assert no .term-tooltip--open remains; assert each span has tabindex="0" and a non-empty data-tooltip attribute.</action>
  <verify>
    <automated>npm test -- tooltips --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tooltips` green: tap opens one tooltip and closes others, Escape closes all, spans carry tabindex="0" + data-tooltip.
    - tooltips.ts toggles only `.term-tooltip--open` via classList (grep present); no `.style.` write and no `setAttribute('style'` (grep absent).
    - The touchstart listener is registered passive and at document level.
    - No data-tooltip value is built from a variable/user input in tooltips.ts (all author-defined static strings).
  </acceptance_criteria>
  <done>Tooltips toggle correctly on tap/keyboard with no inline style; the seven-term contract and the canonical "teruglevering" term are documented for the copy pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author copy → panel DOM + data-tooltip | All panel text and tooltip content are author-defined static strings — no user CSV data path |
| touch/keyboard events → classList state | Untrusted event timing, but only toggles a CSS class; no data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering (XSS) | data-tooltip / panel copy | mitigate | All copy via textContent and author-defined data-tooltip constants; never interpolate user CSV data (Pitfall 8); test asserts no variable-built data-tooltip |
| T-05-09 | Tampering | inline style / CSP | mitigate | Tooltip + panel state is classList-only; no element.style writes; verify greps assert this; display handled by Plan 02 CSS |
| T-05-10 | Information Disclosure | honesty regression (v2 over-promise) | mitigate | transparency-panel test includes a D-09 negative assertion that the no-euros body makes no v2 promise |

No high-severity threats. Static author copy + CSS-class event toggles only; no network, no user-string rendering.
</threat_model>

<verification>
- `npm test` full suite green (panel + tooltip tests + existing suite).
- `npx tsc --noEmit` clean.
- No inline style in either module.
</verification>

<success_criteria>
- Collapsible Dutch assumptions panel with full saldering depth (D-08) and honest no-euros boundary (D-09) — UX-01/UX-02.
- Tap/hover/keyboard tooltips on the seven technical terms, CSP-safe — UX-03/D-11.
- Plan 05 can append the panel and call initTooltips() during wiring.
</success_criteria>

<output>
Create `.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-04-SUMMARY.md` when done.
</output>
