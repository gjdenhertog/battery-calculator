---
phase: 05-visualizations-polish-transparent-assumptions-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/domain/bucket-by-month.ts
  - src/domain/select-representative-week.ts
  - src/helpers/format.ts
  - tests/bucket-by-month.test.ts
  - tests/select-representative-week.test.ts
  - tests/format.test.ts
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-04]
must_haves:
  truths:
    - "uPlot ^1.6.32 is installed and importable without touching the CSP"
    - "bucketByMonth groups TraceRow[] into Amsterdam-local months with shiftedKwh and isPartial flags"
    - "selectRepresentativeWeek returns the Mon–Sun window with the highest residualExportKwh sum, never null"
    - "formatAxisKwh(3.14159) returns '3.1' (1 decimal, no kWh suffix)"
  artifacts:
    - path: "src/domain/bucket-by-month.ts"
      provides: "Pure month-bucketing helper (VIZ-01 data prep)"
      exports: ["bucketByMonth", "MonthBucket"]
      min_lines: 40
    - path: "src/domain/select-representative-week.ts"
      provides: "Pure highest-teruglevering week heuristic (VIZ-02 data prep)"
      exports: ["selectRepresentativeWeek", "RepresentativeWeek"]
      min_lines: 40
    - path: "src/helpers/format.ts"
      provides: "formatAxisKwh added to existing formatters (VIZ-04)"
      contains: "formatAxisKwh"
    - path: "tests/bucket-by-month.test.ts"
      provides: "VIZ-01 fixture lock (full/partial/sparse/DST)"
    - path: "tests/select-representative-week.test.ts"
      provides: "VIZ-02 fixture lock (best week/tie-break/<7 days)"
  key_links:
    - from: "src/domain/bucket-by-month.ts"
      to: "@date-fns/tz TZDate"
      via: "Amsterdam local-month bucketing"
      pattern: "new TZDate\\("
    - from: "src/domain/select-representative-week.ts"
      to: "date-fns startOfWeek + TZDate"
      via: "Monday week-boundary grouping in Amsterdam time"
      pattern: "startOfWeek"
---

<objective>
Install uPlot and build the two pure, node-testable data helpers the Phase 5 charts depend on, plus the one-decimal axis formatter.

Purpose: Charts (Plan 03) must consume already-proven data transforms. Per the established Phase 1/3 pure-core / browser-shell split, month-bucketing (VIZ-01) and the highest-teruglevering week heuristic (VIZ-02) are pure functions with node-env Vitest fixtures — testable without any canvas. This plan is the Wave 0 gate: it installs the one new runtime dependency and lands the helpers + their fixtures so downstream chart adapters have stable contracts.
Output: uPlot installed; `bucket-by-month.ts`, `select-representative-week.ts`, extended `format.ts`, and three test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. From src/domain/types.ts (verified 2026-06-14). -->
TraceRow: { timestamp: Date (UTC, end of interval); socKwh; chargedKwh; dischargedKwh; residualImportKwh >= 0; residualExportKwh >= 0 }
  - residualExportKwh is teruglevering — the field the week heuristic sums.
  - shiftedKwh for a month is NOT a TraceRow field; derive monthly shifted energy from chargedKwh per row (the grid-side energy the battery absorbed = energy shifted into the battery). Sum chargedKwh per month for the bar height (matches SimResult.shiftedKwh semantics: "energy the battery moved from grid to self-use").
SimResult.shiftedKwh: number — the period total; the sum of all monthly bucket shiftedKwh values must reconcile to this within rounding.

Existing format.ts (verified): formatKwh(n) -> "12.3 kWh"; formatPct; formatRatio; formatDate; formatCount. All use n.toFixed(1) style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install uPlot and confirm CSP is untouched</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current deps — confirm uplot is absent)
    - src/constants/csp.ts (the CSP that MUST remain unchanged)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (§"No New Dependencies Beyond uPlot", §Package Legitimacy Audit, §CSP)
  </read_first>
  <action>Run `npm install uplot@^1.6.32` to add uPlot as a runtime dependency. uPlot is the LOCKED charting lib per CLAUDE.md and is the ONLY new runtime dep this phase adds (legitimacy approved in RESEARCH Package Legitimacy Audit: github.com/leeoniya/uPlot, multi-million monthly downloads, cited in CLAUDE.md — disposition Approved, no human checkpoint required). Do NOT modify `src/constants/csp.ts`: uPlot renders to canvas, performs no network fetch, and injects no inline style, so `style-src 'self'`, `connect-src 'none'`, and `worker-src 'self' blob:` all remain intact. After install, confirm `import uPlot from 'uplot'` type-resolves (uPlot ships its own .d.ts).</action>
  <verify>
    <automated>node -e "const v=require('./package.json').dependencies.uplot; if(!v||!v.includes('1.6')) {console.error('uplot missing/wrong version: '+v); process.exit(1)} console.log('uplot '+v)"</automated>
    <automated>grep -q "connect-src 'none'" src/constants/csp.ts && grep -q "style-src 'self'" src/constants/csp.ts && echo "CSP intact"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `uplot` at `^1.6.32` (or a `1.6.x` resolved version).
    - `src/constants/csp.ts` is byte-for-byte unchanged: `connect-src 'none'` and `style-src 'self'` still present, no new directive added.
    - `npx tsc --noEmit` does not report a missing-module error for `'uplot'`.
  </acceptance_criteria>
  <done>uPlot ^1.6.32 installed; CSP unchanged; uplot module type-resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bucketByMonth pure helper + node-env fixtures (VIZ-01)</name>
  <files>src/domain/bucket-by-month.ts, tests/bucket-by-month.test.ts</files>
  <read_first>
    - src/domain/gaps.ts (canonical TZDate Amsterdam walk — copy the import block and `new TZDate(ts.getTime(), AMSTERDAM)` local-fields pattern)
    - src/domain/period-filter.ts (canonical pure-function signature + JSDoc-invariant style)
    - src/domain/types.ts lines 227-283 (TraceRow + SimResult field definitions)
    - tests/dst-fixtures.test.ts (TZDate inline fixture-array construction pattern)
    - tests/format.test.ts (node-env pure-function test header style)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (§"Month Bucketing Across DST", Pitfall 3)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (§ bucket-by-month.ts)
  </read_first>
  <behavior>
    - bucketByMonth(trace, zone) returns one MonthBucket per Amsterdam-local calendar month present in the trace, in chronological order.
    - Each MonthBucket has: monthKey ("2025-06"), monthLabel (abbreviated NL month + 2-digit year, e.g. "jun '25"), shiftedKwh (sum of chargedKwh for rows in that month), isPartial (true if the month's first OR last calendar day is absent from the trace).
    - A timestamp like 2025-03-31T23:30:00Z buckets into April Amsterdam-local (CEST +02:00) — NOT March — proving TZDate (not raw Date.getMonth) is used (DST boundary test).
    - Sparse case: a dataset spanning fewer than 2 full months returns all-partial buckets (isPartial=true for each), never an empty array and never a hidden chart (D-06).
    - The sum of all bucket.shiftedKwh reconciles to the total chargedKwh across the trace within rounding tolerance.
  </behavior>
  <action>Create `src/domain/bucket-by-month.ts` exporting `interface MonthBucket { monthKey: string; monthLabel: string; shiftedKwh: number; isPartial: boolean }` and `export function bucketByMonth(trace: TraceRow[], zone: string): MonthBucket[]`. Import `{ TZDate } from '@date-fns/tz'` and `{ TraceRow } from './types'` following gaps.ts. For each row compute the Amsterdam-local month via `new TZDate(row.timestamp.getTime(), zone).getMonth()` / `.getFullYear()` (Pitfall 3: never raw `Date.getMonth()`). Group rows by `YYYY-MM` key; sum `chargedKwh` per bucket for shiftedKwh (per <interfaces>, chargedKwh is the grid-side energy shifted into the battery). Build monthLabel from a hard-coded NL abbreviated-month array (jan, feb, mrt, apr, mei, jun, jul, aug, sep, okt, nov, dec) plus the 2-digit year. Determine isPartial: a month is full only if both its 1st calendar day and its last calendar day (compute via the month length) appear among the local-day numbers seen in that bucket; otherwise isPartial=true. Return buckets sorted by monthKey ascending. Pure function — no DOM, no side effects, named export only. Write `tests/bucket-by-month.test.ts` (node env, no environment annotation) using inline TZDate-built TraceRow fixtures (factory `makeTraceRow(isoDate, chargedKwh)` per PATTERNS) covering: a full month, a partial first/last month, the DST-boundary bucketing case, and the sparse (<2 full months) case.</action>
  <verify>
    <automated>npm test -- bucket-by-month --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- bucket-by-month` passes with tests covering: full month, partial month, DST boundary (late-evening Amsterdam timestamp buckets to the local month, not UTC), and sparse <2-full-months returns all-isPartial.
    - `bucketByMonth` imports `TZDate` from `@date-fns/tz` (grep `new TZDate(` present; grep `\.getMonth()` on a raw `new Date(` absent).
    - The helper has zero imports from `src/ui/`, `uplot`, or any DOM global (pure node-safe).
    - Sum-reconciliation assertion: summing returned `shiftedKwh` equals total `chargedKwh` in the fixture within `toBeCloseTo` 3 decimals.
  </acceptance_criteria>
  <done>bucketByMonth lands as a pure node-safe helper; all four fixture cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: selectRepresentativeWeek heuristic + formatAxisKwh (VIZ-02, VIZ-04)</name>
  <files>src/domain/select-representative-week.ts, src/helpers/format.ts, tests/select-representative-week.test.ts, tests/format.test.ts</files>
  <read_first>
    - src/domain/gaps.ts (TZDate walk pattern)
    - src/domain/types.ts lines 227-241 (TraceRow.residualExportKwh = teruglevering, the field summed)
    - src/helpers/format.ts (self-extend: copy formatKwh's toFixed(1) style for formatAxisKwh)
    - tests/format.test.ts (extend in place — add formatAxisKwh cases)
    - tests/dst-fixtures.test.ts (TZDate fixture construction)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (§"Highest-Teruglevering Week Heuristic", Assumption A1)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (§ select-representative-week.ts, § format.ts extend)
  </read_first>
  <behavior>
    - selectRepresentativeWeek(trace, zone) groups rows into Mon–Sun weeks in Amsterdam local time and returns the week with the highest sum of residualExportKwh.
    - Tie-break: the earliest (first) week wins when two weeks have equal sums.
    - Returns { startTs: number (ms, Monday 00:00 Amsterdam), endTs: number (ms, Sunday end Amsterdam), weekLabel: string like "8–14 juni 2025" }. Never returns null.
    - Dataset < 7 days: returns the single available span (whatever rows exist) rather than crashing or returning null.
    - formatAxisKwh(3.14159) returns "3.1"; formatAxisKwh(0) returns "0.0"; no " kWh" suffix.
  </behavior>
  <action>Create `src/domain/select-representative-week.ts` exporting `interface RepresentativeWeek { startTs: number; endTs: number; weekLabel: string }` and `export function selectRepresentativeWeek(trace: TraceRow[], zone: string): RepresentativeWeek`. Import `{ TZDate } from '@date-fns/tz'`, `{ startOfWeek } from 'date-fns'`, `{ TraceRow } from './types'`. For each row wrap into `new TZDate(row.timestamp.getTime(), zone)`, compute the Monday boundary with `startOfWeek(local, { weekStartsOn: 1 })`, key weeks by that boundary's timestamp, and accumulate `residualExportKwh` per week. Pick the max-sum week; on ties keep the earliest start. Build weekLabel as the Dutch dated caption "{D}–{D} {maand} {yyyy}" using Amsterdam-local start/end dates and the same NL month-name array as bucket-by-month (reuse via a shared constant or duplicate the small array — planner's discretion, keep it local). Handle the <7-day edge by returning the span of the only group present. Pure, named export, no DOM. Then extend `src/helpers/format.ts` with `export function formatAxisKwh(n: number): string { return n.toFixed(1) }` (same docblock style as formatKwh, no suffix — for compact uPlot axis ticks). Write `tests/select-representative-week.test.ts` (node env) covering: highest-teruglevering week selected across a multi-week fixture, tie-break to first week, and a <7-day dataset returning a single span. Add formatAxisKwh assertions to `tests/format.test.ts`.</action>
  <verify>
    <automated>npm test -- select-representative-week --run --reporter=dot</automated>
    <automated>npm test -- format --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- select-representative-week` passes: best-week, tie-break-first, and <7-day single-span cases.
    - `npm test -- format` passes including `formatAxisKwh(3.14159) === "3.1"` and `formatAxisKwh(0) === "0.0"`.
    - `selectRepresentativeWeek` never returns null (asserted in the <7-day test).
    - `select-representative-week.ts` imports `startOfWeek` from `date-fns` and `TZDate` from `@date-fns/tz`; has zero `src/ui/` or `uplot` imports.
  </acceptance_criteria>
  <done>Week heuristic + formatAxisKwh land; all node-env fixtures green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New dependency (uPlot) enters the bundle; supply-chain surface |
| (no runtime trust boundary in this plan) | Pure helpers consume already-validated in-memory TraceRow[]; no user-string handling, no network, no DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | `npm install uplot` | mitigate | uPlot legitimacy verified in RESEARCH Package Legitimacy Audit (official repo github.com/leeoniya/uPlot, multi-million downloads, cited in CLAUDE.md) — disposition Approved, no [ASSUMED]/[SUS] flag, so no blocking human checkpoint required per the package gate |
| T-05-01 | Tampering | CSP regression on dep add | mitigate | Task 1 verify asserts `connect-src 'none'` and `style-src 'self'` remain in src/constants/csp.ts; uPlot adds no CSP relaxation |

No high-severity threats. This plan adds only pure node-safe logic and one vetted dependency; no user-derived string rendering and no network surface.
</threat_model>

<verification>
- `npm test` full suite green (all new node-env fixtures plus existing suite).
- `npx tsc --noEmit` clean — uplot module resolves; new helpers type-check.
- CSP file unchanged.
</verification>

<success_criteria>
- uPlot ^1.6.32 installed; CSP untouched.
- bucketByMonth, selectRepresentativeWeek, formatAxisKwh exist as pure helpers with green node-env fixtures.
- Downstream chart adapters (Plan 03) can import all three contracts without exploration.
</success_criteria>

<output>
Create `.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-01-SUMMARY.md` when done.
</output>
