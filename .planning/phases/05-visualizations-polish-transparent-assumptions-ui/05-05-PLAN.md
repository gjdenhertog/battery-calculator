---
phase: 05-visualizations-polish-transparent-assumptions-ui
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - src/main.ts
autonomous: false
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, UX-01, UX-02, UX-03, UX-04, UX-05, UX-06]
user_setup: []
must_haves:
  truths:
    - "Both charts, the transparency panel, and tooltip wiring mount inside #results-region below the comparison table"
    - "All four new CSS files are imported so the visuals apply"
    - "The full build succeeds and the dev app renders charts with table-matching colors, step lines, and an honest week caption"
    - "At 375px the results layout reflows to stacked cards with no horizontal scroll for headline numbers"
    - "The full Vitest suite (incl. terminology + no-CTA audits) is green"
  artifacts:
    - path: "src/main.ts"
      provides: "Phase 5 wiring: CSS imports + chart/panel/tooltip init appended to #results-region"
      contains: "initMonthlyBarsChart"
  key_links:
    - from: "src/main.ts"
      to: "#results-region"
      via: "appendChild chart mounts + panel below comparison table"
      pattern: "resultsRegion.appendChild"
    - from: "src/main.ts"
      to: "initTooltips + renderTransparencyPanel"
      via: "Phase 5 init calls"
      pattern: "initTooltips"
---

<objective>
Wire all Phase 5 surfaces into the live app: import the four new CSS files, mount both charts and the transparency panel inside #results-region below the comparison table, initialize tooltips, then verify the integrated result live (charts, colors, step lines, mobile reflow).

Purpose: Plans 01-04 produced isolated, tested units. This plan integrates them into main.ts following the existing CSS-import-then-init wiring pattern, and closes the manual-only verifications from 05-VALIDATION.md (UX-04 375px reflow and the VIZ visual step-line/color check that jsdom cannot assert). The CI audits (UX-05/UX-06) are already enforced by Plan 02's test; this plan confirms the full suite stays green after integration.
Output: extended src/main.ts; a passing build; live verification sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-VALIDATION.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md

<interfaces>
<!-- Verified from src/main.ts (2026-06-14). CSS is imported in main.ts (NOT global.css). -->
Existing main.ts: imports './styles/global.css' + drop-zone/battery-picker/comparison-table/results-region CSS at top; inits initDropZone, initBatteryPicker (into #drop-zone region); initPeriodControl + initComparisonTable into `const resultsRegion = document.getElementById('results-region')` (guarded by `if (resultsRegion)`), appending a comparisonTableMount div then calling initComparisonTable(mount) and storing disposeComparisonTable; HMR teardown via `if (import.meta.hot) import.meta.hot.dispose(...)`.
Phase 5 exports to wire: initMonthlyBarsChart(container)->dispose (src/ui/charts/monthly-bars); initFlowChart(container)->dispose (src/ui/charts/flow-chart); renderTransparencyPanel(): HTMLElement (src/ui/transparency-panel); initTooltips(): void (src/ui/tooltips).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 5 CSS imports + chart/panel/tooltip mounts into main.ts</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (the file being modified — full current state: existing import order + the `if (resultsRegion)` block + HMR dispose)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (src/main.ts extend section — the exact import + append-after-comparison-table pattern)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory ordering: monthly bars, then flow chart, then transparency panel, all below the table)
  </read_first>
  <action>Extend src/main.ts following the self-extend pattern in PATTERNS.md. Add the four CSS imports near the existing style imports: './styles/charts.css', './styles/tooltips.css', './styles/transparency-panel.css', './styles/mobile-reflow.css'. Add the module imports: initMonthlyBarsChart from './ui/charts/monthly-bars', initFlowChart from './ui/charts/flow-chart', renderTransparencyPanel from './ui/transparency-panel', initTooltips from './ui/tooltips'. Inside the existing `if (resultsRegion)` block, AFTER the comparison-table mount (do not replace existing code): create a div#monthly-chart-mount, appendChild to resultsRegion, call initMonthlyBarsChart and store disposeMonthlyBars; create a div#flow-chart-mount, appendChild, call initFlowChart and store disposeFlowChart; call renderTransparencyPanel() and appendChild the returned element; call initTooltips(). Extend the existing `import.meta.hot.dispose` callback to also call disposeMonthlyBars() and disposeFlowChart(). Mount order inside #results-region must be: comparison table → monthly bars → flow chart → transparency panel (UI-SPEC ordering). Do not introduce any inline style; do not edit global.css.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test --run --reporter=dot</automated>
    <automated>node -e "const m=require('fs').readFileSync('src/main.ts','utf8');for(const n of ['./styles/charts.css','./styles/tooltips.css','./styles/transparency-panel.css','./styles/mobile-reflow.css','initMonthlyBarsChart','initFlowChart','renderTransparencyPanel','initTooltips']){if(!m.includes(n)){console.error('main.ts missing '+n);process.exit(1)}}console.log('wiring ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (Vite production build, uPlot bundled, no CSP-affecting output).
    - `npm test` full suite green — including tests/terminology-audit (UX-05/UX-06) after integration.
    - main.ts imports the four new CSS files and the four Phase 5 modules; the chart mounts are appended to resultsRegion AFTER the comparison table.
    - HMR dispose calls disposeMonthlyBars() and disposeFlowChart().
    - global.css is unchanged.
  </acceptance_criteria>
  <done>All Phase 5 surfaces mount in #results-region in the correct order; build and full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — charts, colors, step lines, mobile reflow</name>
  <what-built>
    Phase 5 is fully wired: monthly self-consumption bars, the sample-week energy-flow step-line chart with a battery dropdown and dated week caption, the collapsible "Hoe is dit berekend?" transparency panel with the "Waarom geen euro's?" subsection, technical-term tooltips, and the 375px mobile reflow — all mounted inside #results-region below the comparison table. uPlot is bundled; CSP is unchanged. These are the two manual-only verifications from 05-VALIDATION.md (canvas pixel output and rendered-layout overflow are not assertable in jsdom).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app. Upload a sample CSV (e.g. a HomeWizard P1 export spanning several weeks) and select 2-3 batteries including Sessy 5 kWh.
    2. Monthly bars (VIZ-01): confirm bars appear per month, one color per battery, and that each battery's bar color MATCHES that battery's color in the comparison table above (COMP-04). Confirm partial months show a "(deels)" mark and are NOT scaled up to look like full months. If the dataset is under two full months, confirm the "weinig data" note appears and bars still render.
    3. Flow chart (VIZ-02/VIZ-03): confirm four series (Grid import, Teruglevering, Laden, Ontladen) render as STEP lines (flat segments + vertical jumps), never smooth curves. Confirm the caption reads "Voorbeeldweek: {dates} — de week met de meeste teruglevering in je data." Change the battery dropdown and confirm the chart + caption update and series-1 color tracks the selected battery.
    4. Transparency panel (UX-01/UX-02): expand "Hoe is dit berekend?"; confirm five Dutch assumptions including the full saldering caveat, and a "Waarom geen euro's?" subsection that explains the kWh-only reason with NO promise of a future euro feature.
    5. Tooltips (UX-03): hover a dotted-underlined term on desktop → tooltip shows; tab to it with the keyboard → tooltip shows; on a touch device / device-mode, tap it → tooltip toggles, tap elsewhere → closes.
    6. Mobile (UX-04): open DevTools device toolbar at 375px width. Confirm the comparison table reflows to stacked per-battery cards, the headline "kWh netto-import vermeden" numbers do NOT overflow or cause horizontal page scroll, and the charts stay within their containers (the flow chart may scroll horizontally inside its own wrapper only).
    7. Console: confirm zero CSP violations ("Refused to apply inline style") and zero network requests with user data in the Network tab.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issue (wrong colors, smooth curves, overflow at 375px, CSP error, missing copy) to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → browser | Vite-bundled JS/CSS served under CSP; uPlot included |
| user CSV (already parsed) → charts via signals | Data already validated upstream; charts only read in-memory SimResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | CSP regression at integration | mitigate | Live check step 7 confirms zero "Refused to apply inline style" CSP violations and zero user-data network requests; CSP file untouched since Plan 01 |
| T-05-12 | Information Disclosure | user data leaves device | mitigate | connect-src 'none' enforced; live Network-tab check confirms no requests with user data after load (PRIV-01 continuity) |

No high-severity threats introduced by wiring; integration only mounts already-vetted, CSP-safe units.
</threat_model>

<verification>
- `npm run build` green; `npm test` full suite green (all VIZ/UX automated tests + audits).
- Live human-verify sign-off on the two manual-only items (375px reflow + chart visual fidelity) plus color consistency and CSP cleanliness.
</verification>

<success_criteria>
- Every Phase 5 requirement (VIZ-01..04, UX-01..06) is observable in the running app and backed by green automated tests where assertable.
- Charts match comparison-table colors; step lines never interpolate; week caption is honest; 375px layout has no headline overflow; no CSP violations.
</success_criteria>

<output>
Create `.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-05-SUMMARY.md` when done.
</output>
