---
phase: 05-visualizations-polish-transparent-assumptions-ui
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/ui/charts/monthly-bars.ts
  - src/ui/charts/flow-chart.ts
  - tests/monthly-bars.test.ts
  - tests/flow-chart.test.ts
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04]
must_haves:
  truths:
    - "Monthly bars chart renders one bar series per selected battery, colored to match the comparison table, with partial months marked '(deels)' and no extrapolation"
    - "When <2 full months exist, a 'weinig data' note shows and the chart still renders the partial bars"
    - "Flow chart renders grid import, teruglevering, charge, discharge as STEP lines (never interpolated) for ONE battery selected via a dropdown"
    - "The flow-chart week caption names the dated highest-teruglevering week and the reason"
    - "Charts re-render reactively on signal change and resize without recreating the chart on every update"
    - "All chart numeric text routes through formatAxisKwh/formatKwh (<=1 decimal)"
  artifacts:
    - path: "src/ui/charts/monthly-bars.ts"
      provides: "uPlot grouped/per-battery bar chart DOM adapter (VIZ-01, VIZ-04)"
      exports: ["initMonthlyBarsChart"]
      min_lines: 80
    - path: "src/ui/charts/flow-chart.ts"
      provides: "uPlot 4-series step-line chart adapter + battery dropdown (VIZ-02, VIZ-03)"
      exports: ["initFlowChart"]
      min_lines: 90
    - path: "tests/monthly-bars.test.ts"
      provides: "jsdom mount + DOM-contract + XSS test"
    - path: "tests/flow-chart.test.ts"
      provides: "jsdom mount + stepped-path config + dropdown + XSS test"
  key_links:
    - from: "src/ui/charts/monthly-bars.ts"
      to: "bucketByMonth + simResults signal"
      via: "effect() subscription → uPlot.setData"
      pattern: "bucketByMonth"
    - from: "src/ui/charts/flow-chart.ts"
      to: "selectRepresentativeWeek + uPlot.paths.stepped"
      via: "stepped path builder over week trace"
      pattern: "stepped"
    - from: "src/ui/charts/*"
      to: "colorFor + getComputedStyle"
      via: "resolve var(--color-battery-N) to hex at mount"
      pattern: "getComputedStyle"
---

<objective>
Build the two uPlot chart DOM adapters that mount inside #results-region and render reactively from the signals store: the monthly self-consumption bars (VIZ-01) and the sample-week energy-flow step-line chart (VIZ-02/03).

Purpose: This is the browser-shell half of the pure-core/browser-shell split. The pure data transforms already exist (Plan 01); these adapters mount uPlot, resolve per-battery colors to match the comparison table (COMP-04), and subscribe to signals via the established comparison-table.ts effect() pattern. All honesty rules are acceptance criteria: partial-month "(deels)" marking with no extrapolation (D-05), sparse "weinig data" note (D-06), step lines never curves (VIZ-03/D-02), and the dated week caption with its heuristic reason (D-03).
Output: src/ui/charts/monthly-bars.ts, src/ui/charts/flow-chart.ts, and their jsdom tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md

<interfaces>
<!-- Verified contracts from src/state/app-state.ts + helpers (2026-06-14). Import from '../../state/app-state' (charts live in src/ui/charts/). -->
Signals (re-exported from app-state.ts): simResults: Signal<SimResult[] | null>; activeBatteries: Signal<BatteryConfig[]> (computed, ordered active selection — use for colorFor ordering AND for the dropdown); isComputing: Signal<boolean>.
colorFor(batteryId: string, orderedSelection: string[]): string  -> 'var(--color-battery-N)'
colorSlotFor(batteryId: string, orderedSelection: string[]): number -> 1..5
formatKwh(n)->"12.3 kWh"; formatAxisKwh(n)->"3.1" (added in Plan 01)
bucketByMonth(trace: TraceRow[], zone: string): MonthBucket[]  // MonthBucket: { monthKey; monthLabel; shiftedKwh; isPartial }
selectRepresentativeWeek(trace: TraceRow[], zone: string): RepresentativeWeek  // { startTs; endTs; weekLabel }
SimResult: { shiftedKwh; residualImportKwh; residualExportKwh; totalImportKwh; totalExportKwh; periodDays; coarseCadenceWarning; trace: TraceRow[] }
TraceRow: { timestamp: Date; socKwh; chargedKwh; dischargedKwh; residualImportKwh; residualExportKwh }
BatteryConfig: has id: string and name: string (name may be a custom/user-derived string — render via textContent).
SimResult[] and activeBatteries are index-aligned: simResults.value[i] is the result for activeBatteries.value[i].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: monthly-bars.ts uPlot bar chart adapter (VIZ-01, VIZ-04)</name>
  <files>src/ui/charts/monthly-bars.ts, tests/monthly-bars.test.ts</files>
  <read_first>
    - src/ui/comparison-table.ts (THE canonical analog: init*() returning dispose, effect() subscription lines ~422-480, .textContent XSS-safe DOM, .results-error error state, class-based dim state)
    - src/helpers/color.ts (colorFor / colorSlotFor signatures)
    - src/domain/bucket-by-month.ts (Plan 01 output — MonthBucket shape)
    - tests/comparison-table.test.ts (jsdom test structure: beforeEach/afterEach signal reset, makeSimResult factory, XSS test)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (Pattern 1 init, Pattern 3 grouped bars + Pitfall 5, Pattern 5 resize, Pattern 6 setData, Pattern 7 color resolution, Open Question 1/2/3)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 1, Color "Monthly bars chart" opacity rules)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (monthly-bars.ts section, uPlot mock requirement)
  </read_first>
  <behavior>
    - initMonthlyBarsChart(container) returns a dispose function (the effect disposer).
    - With simResults null or activeBatteries empty: renders a placeholder/empty state, no chart, no crash.
    - With N batteries: builds bar data via bucketByMonth(result.trace, 'Europe/Amsterdam') per battery; x-axis = month ordinals with monthLabel tick labels; one bar series per battery using colorFor()-resolved hex.
    - Partial months (bucket.isPartial) render at lower fill opacity AND get a "(deels)" annotation as a .chart-partial-label span below the axis — NEVER scaled up to a full-month estimate (no extrapolation).
    - When <2 full months exist (count buckets where !isPartial), a .chart-sparse-note paragraph with the "weinig data" copy appears and the partial bars still render.
    - Axis tick kWh labels route through formatAxisKwh (no raw multi-decimal float in the rendered DOM/legend).
    - On signal re-update the chart calls setData (does not new-up uPlot every time); destroy+recreate only when battery COUNT changes. ResizeObserver created once, guarded with `if (chart)`.
    - A custom battery name containing '<script>' never produces a <script> node in the legend (textContent only).
  </behavior>
  <action>Create src/ui/charts/monthly-bars.ts exporting `initMonthlyBarsChart(container: HTMLElement): () => void`, following comparison-table.ts structure. Import effect from '@preact/signals-core'; simResults, activeBatteries, isComputing from '../../state/app-state'; colorFor, colorSlotFor from '../../helpers/color'; formatKwh, formatAxisKwh from '../../helpers/format'; bucketByMonth from '../../domain/bucket-by-month'; types from '../../domain/types'; uPlot from 'uplot' (and `import 'uplot/dist/uPlot.min.css'` if the executor confirms uPlot ships a stylesheet that Vite can import without a CSP inline-style violation — uPlot's CSS is a bundled stylesheet file, served under style-src 'self', NOT an inline style; acceptable). Build the section DOM: a .chart-section with .results-section-heading "Zelfverbruik per maand", a #chart-monthly .chart-wrapper for the canvas, a .chart-legend, and containers for .chart-partial-label / .chart-sparse-note. Resolve battery colors with a resolveBatteryColor(id, orderedIds) helper that strips `var(...)` from colorFor() and reads getComputedStyle(document.documentElement).getPropertyValue (Pattern 7 / Pitfall 2). For the bar layout choose the approach per RESEARCH Open Question 1: default to uPlot.paths.bars() with one series per battery; if true side-by-side grouping needs the demo seriesBarsPlugin (quadtree/distr — NOT in the npm package, Pitfall 5), fall back to the documented simpler approach (per-battery sub-position via disp, or stacked-vertical single-battery charts) — state the chosen approach in the SUMMARY. x-axis tick labels via axes[0].values mapping ordinal index → monthLabel. y-axis tick values via formatAxisKwh. Full months at higher fill opacity, partial months lower opacity per UI-SPEC Color (use the same battery color, lower alpha — color-mix or rgba); add the "(deels)" label DOM. Sparse note appears when full-month count < 2. Use the effect() pattern: create uPlot once, setData on update, destroy+recreate only on battery-count change; ResizeObserver (once, guarded, 100ms debounce, setSize height 280). Legend rows built via DOM with .battery-swatch--{slot} swatch class (from colorSlotFor) and battery.name via textContent. Errors caught → .results-error. Write tests/monthly-bars.test.ts (// @vitest-environment jsdom) with the comparison-table.test.ts structure: vi.mock('uplot') returning a stub with setData/setSize/destroy/root; assert empty state when simResults null; assert section heading + #chart-monthly present after a SimResult with multi-month trace; assert formatAxisKwh used (no raw float in legend/labels); assert sparse-note appears for a <2-full-month fixture; assert a '<script>' battery name yields zero <script> nodes.</action>
  <verify>
    <automated>npm test -- monthly-bars --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- monthly-bars` green: empty state, mounted state, formatAxisKwh usage, sparse-note presence, XSS-safe legend.
    - monthly-bars.ts imports bucketByMonth and colorFor; resolves colors via getComputedStyle (grep getComputedStyle present).
    - No raw `Date.getMonth()` bucketing in the adapter (bucketing is delegated to the pure helper).
    - uPlot is created once and updated via setData (grep `setData` present; chart instance stored outside the effect callback).
    - No inline style assignment: grep absent for `.style.` property writes and `setAttribute('style'`.
    - The sparse-note copy matches the UI-SPEC "Weinig data — ..." text.
  </acceptance_criteria>
  <done>Monthly bars adapter renders reactively, colors match the table, partials marked, sparse note honest; all jsdom tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flow-chart.ts uPlot step-line adapter + battery dropdown (VIZ-02, VIZ-03)</name>
  <files>src/ui/charts/flow-chart.ts, tests/flow-chart.test.ts</files>
  <read_first>
    - src/ui/comparison-table.ts (effect()/dispose pattern, .textContent, .results-error)
    - src/ui/drop-zone.ts lines ~207-250 (addEventListener('change'/event) pattern for the dropdown)
    - src/domain/select-representative-week.ts (Plan 01 output — RepresentativeWeek shape)
    - src/helpers/color.ts (colorFor)
    - tests/comparison-table.test.ts (jsdom structure + signal reset)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (Pattern 2 stepped paths, Pattern 4 tzDate, Pitfall 1 seconds-not-ms, Pitfall 4 no built-in tooltip, Pitfall 6 effect cleanup)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 2, the four-series color table, week caption copy, legend labels)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (flow-chart.ts section)
  </read_first>
  <behavior>
    - initFlowChart(container) returns a dispose function.
    - Renders ONE battery's flows at a time, chosen via a <select> dropdown listing all active batteries (options via textContent), defaulting to the first-selected battery (D-01).
    - Filters the selected battery's trace to the highest-teruglevering week via selectRepresentativeWeek(trace, 'Europe/Amsterdam'); renders four step-line series: grid import (residualImportKwh, battery slot color), teruglevering (residualExportKwh, --color-text-muted), laden (chargedKwh, green), ontladen (dischargedKwh, amber).
    - All four series use uPlot.paths.stepped({ align: 1 }) — never smooth interpolation (VIZ-03).
    - x-axis timestamps are Unix SECONDS (timestamp.getTime()/1000), displayed in Europe/Amsterdam via tzDate: ts => uPlot.tzDate(ts, 'Europe/Amsterdam') (Pitfall 1).
    - Week caption text: "Voorbeeldweek: {weekLabel} — de week met de meeste teruglevering in je data." updated on dropdown change.
    - Changing the dropdown re-renders with the new battery's trace, week, and series-1 color; new SimResult[] arrivals re-render (default to first if the selected battery left the selection).
    - Does NOT use uPlot's built-in cursor tooltip (Pitfall 4 — would inject inline style). Custom legend below canvas via DOM.
  </behavior>
  <action>Create src/ui/charts/flow-chart.ts exporting `initFlowChart(container: HTMLElement): () => void`. Same imports as monthly-bars (swap bucketByMonth for selectRepresentativeWeek). Build the .chart-section with .chart-section__header (heading "Energiestroom voorbeeldweek" + a .chart-battery-select__label wrapping a <select id="flow-chart-battery" class="chart-battery-select">), a .chart-week-caption paragraph, a #chart-flow .chart-wrapper, and a .chart-legend. Populate the <select> options from activeBatteries (option.value=id, option.textContent=name); wire select.addEventListener('change', ...) to re-render for the chosen id. Track the selected battery id in a module-local variable defaulting to activeBatteries.value[0].id. In the effect(): read simResults + activeBatteries; map the selected id to its index → SimResult; run selectRepresentativeWeek on that trace; slice the trace rows whose timestamp ms falls within [startTs, endTs]; build AlignedData with data[0]=rows.map(r=>r.timestamp.getTime()/1000) (SECONDS) and data[1..4]=residualImportKwh/residualExportKwh/chargedKwh/dischargedKwh. uPlot opts: tzDate: ts=>uPlot.tzDate(ts,'Europe/Amsterdam'); axes y values via formatAxisKwh; series labels "Grid import"/"Teruglevering"/"Laden"/"Ontladen"; each series paths: uPlot.paths.stepped({align:1}), points:{show:false}, width 2; series-1 stroke = resolveBatteryColor(selectedId), series-2 = resolved --color-text-muted, series-3 = '#16a34a', series-4 = '#d97706' (per UI-SPEC color table). Create uPlot once; setData on update; destroy+recreate only when series structure changes; ResizeObserver once (guarded, debounced, setSize height 320). Update the .chart-week-caption via textContent. Do NOT enable uPlot's built-in tooltip. Build the legend via DOM. Errors → .results-error. Write tests/flow-chart.test.ts (// @vitest-environment jsdom) with vi.mock('uplot') (stub must also expose paths.stepped as a vi.fn and a tzDate static): assert dispose returned; assert <select id="flow-chart-battery"> with one option per active battery; assert the stepped path builder is requested (uPlot.paths.stepped called) — proving VIZ-03; assert week caption text contains "Voorbeeldweek:" and "teruglevering"; assert changing select.value + dispatching 'change' updates the caption; assert a '<script>' battery name yields zero <script> nodes in the dropdown.</action>
  <verify>
    <automated>npm test -- flow-chart --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- flow-chart` green: dispose, dropdown options, stepped-path requested, caption text, change re-render, XSS-safe options.
    - flow-chart.ts calls uPlot.paths.stepped (grep present) and never sets a smooth/spline path (grep absent for `spline`).
    - x-axis uses seconds: grep `getTime() / 1000` (or `getTime()/1000`) present.
    - tzDate uses 'Europe/Amsterdam' (grep present).
    - uPlot built-in tooltip NOT enabled: grep absent for `tooltip: true` and `cursor:` with a tooltip plugin.
    - No inline style assignment (grep absent for `.style.` writes / `setAttribute('style'`).
  </acceptance_criteria>
  <done>Flow chart renders four step-line series for one dropdown-selected battery over the highest-teruglevering week with an honest dated caption; all jsdom tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BatteryConfig.name (possibly user-defined custom battery) → chart legend/dropdown DOM | Untrusted string crosses into the DOM |
| uPlot canvas render | uPlot draws to canvas; must not inject inline style or fetch resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (XSS) | chart legend + flow-chart dropdown options | mitigate | All battery names rendered via .textContent (comparison-table.ts convention); both tests include a '<script>' name assertion proving zero script nodes |
| T-05-06 | Tampering | uPlot inline-style / CSP regression | mitigate | Do NOT enable uPlot built-in cursor tooltip (Pitfall 4); use CSS-class state only; verify greps assert no `.style.` writes; setSize sets canvas attributes not inline style (CSP-safe) |
| T-05-07 | Information Disclosure | uPlot network fetch | accept | uPlot is fully bundled and renders to canvas with system font; connect-src 'none' blocks any fetch attempt at runtime — no code path initiates one |

No high-severity threats. The only untrusted input is a battery name string, mitigated by the established textContent convention with test coverage.
</threat_model>

<verification>
- `npm test` full suite green (both chart tests + existing suite).
- `npx tsc --noEmit` clean.
- Chart colors resolve from CSS custom properties (COMP-04 consistency with the table).
</verification>

<success_criteria>
- Monthly bars (VIZ-01) and flow step-line chart (VIZ-02/03) render reactively with table-matching colors, partial/sparse honesty, and <=1-decimal numbers (VIZ-04).
- Plan 05 can mount both adapters into #results-region without further chart logic.
</success_criteria>

<output>
Create `.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-03-SUMMARY.md` when done.
</output>
