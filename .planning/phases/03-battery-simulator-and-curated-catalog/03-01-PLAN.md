---
phase: 03-battery-simulator-and-curated-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/battery-catalog.ts
  - tests/catalog.test.ts
autonomous: true
requirements: [BATT-01, BATT-02, BATT-03]
must_haves:
  truths:
    - "Every downstream domain file can import BatteryConfig, TraceRow, SimResult, SimOptions from src/domain/types.ts"
    - "The catalog loads 6-8 NL battery entries with Sessy 5 kWh first/default"
    - "Each catalog entry carries all five physics fields plus a datasheet URL"
    - "D-01: SimResult exposes energy aggregates (shiftedKwh, residual/total import & export, periodDays) AND a per-interval trace[] of TraceRow — the locked Phase 4/5 contract is defined here"
    - "D-08: battery capacity is stored as nominalCapacityKwh + dodFraction so the sim derives usable = nominal x dod (criterion 3 direct assertion); custom config mirrors the same five physics fields"
    - "D-09: catalog ships the ROADMAP starter set — Sessy 5 (default-first), Sessy 10, Zonneplan, Tesla Powerwall 3, Huawei LUNA2000, Victron ESS, plus Marstek Venus E as the +1"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "BatteryConfig, TraceRow, SimResult, SimOptions interfaces (the Phase 4/5 contract)"
      contains: "export interface BatteryConfig"
    - path: "src/domain/battery-catalog.ts"
      provides: "BATTERY_CATALOG typed array, Sessy 5 first"
      contains: "BATTERY_CATALOG"
    - path: "tests/catalog.test.ts"
      provides: "Catalog shape + Sessy-first + datasheet-URL assertions"
      contains: "BATTERY_CATALOG"
  key_links:
    - from: "src/domain/battery-catalog.ts"
      to: "src/domain/types.ts"
      via: "import type { BatteryConfig }"
      pattern: "import type.*BatteryConfig.*from './types'"
    - from: "tests/catalog.test.ts"
      to: "src/domain/battery-catalog.ts"
      via: "import { BATTERY_CATALOG }"
      pattern: "BATTERY_CATALOG.*from.*battery-catalog"
---

<objective>
Define the Phase 3 type contracts on `src/domain/types.ts` (BatteryConfig, TraceRow,
SimResult, SimOptions) and ship the curated NL battery catalog as a typed `.ts` export
with a contract-locking test. This is the interface-first foundation: every later plan
imports these types and (for fixtures) the catalog.

Purpose: Establishes the locked Phase 4/5 contract (D-01/D-08) and the BATT-01..03
catalog before any simulation code consumes it, so executors of plans 02/03 receive the
contracts in-hand rather than exploring the codebase.

Output: Extended `types.ts`, new `battery-catalog.ts`, new `tests/catalog.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-battery-simulator-and-curated-catalog/03-CONTEXT.md
@.planning/phases/03-battery-simulator-and-curated-catalog/03-RESEARCH.md
@.planning/phases/03-battery-simulator-and-curated-catalog/03-PATTERNS.md
@src/domain/types.ts

<interfaces>
<!-- Locked contract shapes (RESEARCH.md §Code Examples, lines 438-475; PATTERNS.md lines 63-97). -->
<!-- Field names are Claude's discretion per D-01/D-08, but these ARE the contract Phase 4/5 reads. -->
<!-- Existing types.ts style to match: `export interface`, JSDoc on every field, invariant -->
<!-- notes referencing decision/requirement IDs. See IntervalSample (types.ts lines 25-32). -->

BatteryConfig: { id: string; name: string; nominalCapacityKwh: number; dodFraction: number;
  roundTripEfficiency: number; maxChargeKw: number; maxDischargeKw: number; datasheetUrl: string }

TraceRow: { timestamp: Date; socKwh: number; chargedKwh: number; dischargedKwh: number;
  residualImportKwh: number; residualExportKwh: number }

SimResult: { shiftedKwh: number; residualImportKwh: number; residualExportKwh: number;
  totalImportKwh: number; totalExportKwh: number; periodDays: number;
  coarseCadenceWarning: boolean; trace: TraceRow[] }

SimOptions: { coarseCadenceThresholdMinutes?: number }
</interfaces>

<catalog_specs>
<!-- The 7-entry datasheet-sourced lineup from RESEARCH.md lines 178-233. Sessy 5 MUST be index 0. -->
<!-- Convention (Pitfall 2): vendors quoting "usable" capacity (Sessy/Tesla/Huawei) use dodFraction: 1.0 — do NOT double-discount. -->

id=sessy-5        name="Sessy 5 kWh"                            nominal=5.0   dod=1.0   rte=0.85   charge=2.2  discharge=1.7   url=https://www.sessy.nl/specificaties/
id=sessy-10       name="Sessy 10 kWh"                           nominal=10.0  dod=1.0   rte=0.85   charge=2.2  discharge=1.7   url=https://www.sessy.nl/sessy-10-kwh/
id=zonneplan-10   name="Zonneplan Thuisbatterij 10 kWh"         nominal=10.0  dod=0.95  rte=0.90   charge=4.4  discharge=4.4   url=https://www.zonneplan.nl/thuisbatterij/10-kwh-thuisbatterij
id=powerwall-3    name="Tesla Powerwall 3"                      nominal=13.5  dod=1.0   rte=0.975  charge=5.0  discharge=10.0  url=https://energylibrary.tesla.com/docs/Public/EnergyStorage/Powerwall/3/Datasheet/en-uk/Powerwall-3-Datasheet-EN.pdf
id=huawei-luna-5  name="Huawei LUNA2000-5-S0"                   nominal=5.0   dod=1.0   rte=0.95   charge=2.5  discharge=2.5   url=https://solar.huawei.com/en/products/luna2000-5-10-15-s0/specs/
id=victron-ess-10 name="Victron ESS (MultiPlus-II + 10 kWh)"    nominal=10.0  dod=0.90  rte=0.85   charge=3.0  discharge=3.0   url=https://www.victronenergy.com/live/ess:start
id=marstek-venus-e name="Marstek Venus E 5,12 kWh"              nominal=5.12  dod=0.95  rte=0.825  charge=2.5  discharge=2.5   url=https://www.marstek.nl/product/marstek-venus-e-3-0-plug-charge-thuisbatterij-5-12-kwh-incl-p1-meter/

<!-- Powerwall discharge = 10 kW (on-grid NL realism, per A-4). Several RTE/DoD figures are -->
<!-- [ASSUMED] defensible v1 defaults (A-2, A-3, A-5, A-6, A-7) — accepted as documented, since -->
<!-- this is a consumer sizing gut-check, not a billing engine. See <slopcheck_gate> below. -->
</catalog_specs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types.ts with the Phase 3 contract interfaces</name>
  <files>src/domain/types.ts</files>
  <read_first>
    - src/domain/types.ts (the file being modified; match the existing `export interface` + per-field JSDoc + invariant-note style of IntervalSample at lines 25-32 and ParseRowError at lines 138-156)
    - .planning/phases/03-battery-simulator-and-curated-catalog/03-PATTERNS.md (lines 33-117 — exact shapes + the "add beside existing interfaces, do NOT create a new types file" rule)
    - .planning/phases/03-battery-simulator-and-curated-catalog/03-RESEARCH.md (lines 438-475 — the locked interface shapes; D-01/D-08 field rationale)
  </read_first>
  <action>
    Append four interfaces to src/domain/types.ts (do NOT create a new file — this is the root
    of the domain type graph, D-01/D-08): `BatteryConfig` (fields id, name, nominalCapacityKwh,
    dodFraction, roundTripEfficiency, maxChargeKw, maxDischargeKw, datasheetUrl), `TraceRow`
    (timestamp, socKwh, chargedKwh, dischargedKwh, residualImportKwh, residualExportKwh),
    `SimResult` (shiftedKwh, residualImportKwh, residualExportKwh, totalImportKwh, totalExportKwh,
    periodDays, coarseCadenceWarning, trace: TraceRow[]), and `SimOptions`
    (coarseCadenceThresholdMinutes?: number). Reuse the UTC-Date convention (DATA-07) for
    TraceRow.timestamp. Add per-field JSDoc citing requirement/decision IDs: dodFraction "1.0 when
    vendor quotes usable (D-08)", roundTripEfficiency "0..1, sqrt() applied each way (SIM-03)",
    chargedKwh "grid-side (criterion 2 -> 0.55); see A-1", coarseCadenceWarning "D-04",
    datasheetUrl "BATT-01: cited in source". Do NOT add a signed `net` field anywhere (DATA-06
    anti-pattern). Do NOT modify or remove IntervalSample / ParseFileResult / MergeResult.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/domain/types.ts contains `export interface BatteryConfig` with all 8 fields (id, name, nominalCapacityKwh, dodFraction, roundTripEfficiency, maxChargeKw, maxDischargeKw, datasheetUrl)
    - src/domain/types.ts contains `export interface TraceRow`, `export interface SimResult` (with `trace: TraceRow[]` and `coarseCadenceWarning: boolean`), and `export interface SimOptions`
    - The existing IntervalSample interface is unchanged (grep `export interface IntervalSample` still present with timestamp/gridImportKwh/gridExportKwh)
    - No `net` field is introduced on any stored-data interface (DATA-06)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Four new interfaces exported from types.ts; tsc clean; IntervalSample untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create the curated battery catalog and its contract test</name>
  <files>src/domain/battery-catalog.ts, tests/catalog.test.ts</files>
  <read_first>
    - src/domain/types.ts (BatteryConfig — the shape the catalog must satisfy; written in Task 1)
    - src/domain/merge.ts (lines 1-17 — the pure-domain file-header house style to replicate)
    - tests/registry.test.ts (the analog for asserting on a typed exported data structure + its entries)
    - .planning/phases/03-battery-simulator-and-curated-catalog/03-PATTERNS.md (lines 218-247 catalog file shape + header convention; lines 336-363 the catalog test assertions)
    - .planning/phases/03-battery-simulator-and-curated-catalog/03-RESEARCH.md (lines 176-236 — the 7-entry table + per-model sourcing + the usable-vs-DoD convention)
  </read_first>
  <action>
    Create src/domain/battery-catalog.ts exporting `export const BATTERY_CATALOG: readonly
    BatteryConfig[] = [ ... ] as const`, importing `import type { BatteryConfig } from './types'`.
    Open with the pure-domain block-comment header (filename, purpose "curated NL battery catalog
    (BATT-01..03)", the line "Pure data — bundled at build time, no fetch (CSP connect-src
    'none')"), and document the usable-vs-DoD convention: dodFraction = 1.0 when the vendor quotes
    usable capacity (Sessy/Tesla/Huawei) — do NOT double-discount. Populate the 7 entries verbatim
    from the <catalog_specs> block above; Sessy 5 (`sessy-5`) MUST be index 0 (BATT-03). Each entry
    gets a trailing `// datasheet: <url>` style comment is optional since datasheetUrl is a field;
    keep the field values exactly as specified (Powerwall discharge 10.0).
    Then create tests/catalog.test.ts (node env, no jsdom override): import { BATTERY_CATALOG } and
    type BatteryConfig. Assert (a) length >= 6 and <= 8; (b) BATTERY_CATALOG[0].id === 'sessy-5'
    (BATT-03); (c) every entry: nominalCapacityKwh > 0, 0 < dodFraction <= 1, 0 < roundTripEfficiency
    <= 1, maxChargeKw > 0, maxDischargeKw > 0, datasheetUrl matches /^https?:\/\//; (d) all ids are
    unique. Open the test with the standard docblock + "Runs in the DEFAULT node environment".
  </action>
  <verify>
    <automated>npx vitest run tests/catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/catalog.test.ts -t "Sessy"` (or equivalent) asserts BATTERY_CATALOG[0].id === 'sessy-5'
    - Catalog has between 6 and 8 entries inclusive; all ids unique
    - Every entry passes the all-five-physics-fields + datasheetUrl URL-format assertion (BATT-02)
    - battery-catalog.ts opens with the pure-domain header and documents the dodFraction usable-vs-DoD convention
    - Sessy/Tesla/Huawei entries have dodFraction === 1.0 (no double-discount)
    - `npx vitest run tests/catalog.test.ts` exits 0
  </acceptance_criteria>
  <done>Catalog ships 7 entries, Sessy 5 first, all-fields test green, tsc clean.</done>
</task>

</tasks>

<slopcheck_gate>
This phase installs ZERO new packages (RESEARCH.md §Package Legitimacy Audit — N/A). No
npm/pip/cargo install task exists in any Phase 3 plan, so no slopcheck / package-legitimacy
checkpoint is required. The catalog is a hand-written, code-reviewed static `.ts` file.

The catalog's `[ASSUMED]` spec figures (A-2, A-3, A-5, A-6, A-7 in RESEARCH.md) are accepted as
documented v1 defaults — this is a consumer sizing gut-check, not a billing engine, and every
figure is a real-world-plausible mid-point with a cited datasheetUrl. Accuracy of a cited number
against its datasheet is a manual code-review spot-check (03-VALIDATION.md §Manual-Only), not an
automatable assertion, and not a blocking gate.
</slopcheck_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author → bundled catalog | The catalog .ts is authored and code-reviewed; it ships as static bundle data with no runtime fetch (CSP connect-src 'none'). |

No runtime trust boundary is crossed in this plan: no network, no I/O, no DOM, no user input — the catalog is static data and types are declarations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | battery-catalog.ts spec figures | accept | Honest v1 defaults; each entry cites a datasheetUrl; accuracy is a code-review spot-check (03-VALIDATION.md). Not a billing engine — a wrong RTE produces a slightly-off gut-check, no security impact. |
| T-03-02 | Information disclosure | catalog data | accept | Static public battery specs; no PII, no secrets, no network. CSP connect-src 'none' (Phase 1) forbids exfiltration. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan (zero new deps, RESEARCH.md audit N/A). Nothing to vet. |

The realistic threat surface for a static-data + type-declaration plan is effectively nil; no HIGH-severity threats exist to mitigate.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (types compile, no breakage to existing domain types)
- `npx vitest run tests/catalog.test.ts` green (BATT-01..03)
- `npm test` still fully green (no regression to Phase 1/2 suites)
- Manual (code review, non-blocking): each catalog entry's specs are plausible against its datasheetUrl
</verification>

<success_criteria>
- BatteryConfig, TraceRow, SimResult, SimOptions exported from types.ts and importable downstream (ROADMAP criterion 5 contract; D-01/D-08)
- Catalog loads 6-8 NL entries, Sessy 5 kWh first/default, each with all five physics fields + datasheet URL (BATT-01, BATT-02, BATT-03; ROADMAP criterion 5)
- Sessy/Tesla/Huawei use dodFraction 1.0 (no double-discount, Pitfall 2)
</success_criteria>

<output>
Create `.planning/phases/03-battery-simulator-and-curated-catalog/03-01-SUMMARY.md` when done
</output>
