---
phase: 06-post-v1-ux-enhancements-multiple-custom-batteries-and-option
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/ui/comparison-table.ts
  - src/main.ts
  - src/styles/comparison-table.css
  - tests/comparison-table.test.ts
autonomous: false
requirements: [COMP-05, COMP-06]
user_setup: []

must_haves:
  truths:
    - "Saldering is OFF by default: first render shows a single 'kWh netto-import vermeden' column with NO zonder/met sub-labels (D-07)"
    - "A labelled checkbox in an options row directly above the table toggles saldering ON; default unchecked (D-06)"
    - "Toggling ON splits the headline into the zonder | met pair (the Phase 4 layout) (D-07)"
    - "The corrected SALDERING_DISCLAIMER_COPY and the info (i) affordance are emitted ONLY when saldering is ON; the OFF view carries no saldering verbiage (D-08)"
    - "The negative-ON note appears only when saldering is ON and some avoidedOn <= 0 (D-08)"
    - "Per-row leader highlighting applies to whichever headline column(s) are visible (D-07)"
    - "The options row survives table re-renders (sits outside the container that renderTable clears) and the toggle recomputes through the existing Comlink worker (D-10)"
  artifacts:
    - path: "src/ui/comparison-table.ts"
      provides: "salderingOn-driven conditional single-vs-pair layout + ON-only disclaimer/note; renderTable takes showSaldering"
      contains: "salderingOn"
    - path: "src/main.ts"
      provides: "saldering options-row mount as a sibling above the comparison-table mount"
      contains: "saldering"
    - path: "src/styles/comparison-table.css"
      provides: "options-row + toggle styling (no inline styles)"
      contains: "options"
    - path: "tests/comparison-table.test.ts"
      provides: "jsdom tests for OFF single-column, ON pair, ON-only disclaimer/note, toggle wiring"
      contains: "salderingOn"
  key_links:
    - from: "src/ui/comparison-table.ts initComparisonTable effect"
      to: "salderingOn.value"
      via: "effect read so the table re-renders on toggle"
      pattern: "salderingOn\\.value"
    - from: "src/main.ts options row checkbox"
      to: "salderingOn signal + scheduleRecompute"
      via: "change handler writes salderingOn.value and calls scheduleRecompute(true)"
      pattern: "salderingOn"
---

<objective>
Make the saldering treatment an opt-in mode that is OFF by default: a single headline column when off, the zonder|met pair when on, with the corrected disclaimer and negative-ON note surfaced only when on. Wire a persistent options-row toggle above the table and live-verify the full worker pipeline for BOTH phase behaviors.

Purpose: Implements D-06 (options-row toggle checkbox, default unchecked), D-07 (single-vs-pair conditional layout + leader highlight on visible columns), D-08 (disclaimer + negative-ON note only when ON), D-10 (no inline styles, recompute through existing worker). This plan owns comparison-table.ts + main.ts exclusively and runs in parallel with 06-02 (which owns battery-picker.ts). The final task is a blocking live human-verify that exercises the Comlink round-trip the test suite cannot (worker-mock blind spot) — covering BOTH the multiple-custom add/remove (from 06-02) and the saldering toggle re-render.

Output: salderingOn-driven conditional table, persistent options-row toggle in main.ts, supporting CSS, jsdom tests, and a live verification.

CONSTRAINT: Do NOT reword SALDERING_DISCLAIMER_COPY, CADENCE_BANNER_COPY, or NEGATIVE_ON_NOTE_COPY — reuse verbatim; this phase only gates their visibility (CONTEXT <domain>).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-CONTEXT.md
@.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md
@.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-01-SUMMARY.md

<interfaces>
<!-- Signals from 06-01 the table consumes. Use directly. -->
From src/state/app-state.ts (after 06-01):
```typescript
export const salderingOn: Signal<boolean>   // default false
export const simResults: Signal<SimResult[] | null>
export const activeBatteries: ReadonlySignal<BatteryConfig[]>
export function scheduleRecompute(immediate?: boolean): void
```
Current renderTable / buildThead / buildBatteryRow signatures (to extend with showSaldering):
```typescript
function buildThead(table: HTMLTableElement): void              // → add (table, showSaldering)
function buildBatteryRow(battery, result, rowIdx, orderedIds, leaders): HTMLTableRowElement  // → add showSaldering
function renderTable(container, results, batteries): void       // → add showSaldering
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conditional single-vs-pair table layout + ON-only disclaimer/note driven by salderingOn</name>
  <files>src/ui/comparison-table.ts, tests/comparison-table.test.ts</files>
  <read_first>
    - src/ui/comparison-table.ts (full file — `buildThead` lines 100-180 with the saldering group header lines 113-127 and the zonder/met sub-label row lines 162-177; `buildBatteryRow` saldering columns lines 241-271 (avoidedOff always, avoidedOn ON-only); `renderTable` lines 341-406 incl. disclaimer block 387-393, wireSalderingDisclaimer call 396, negative-ON note 398-405; the `initComparisonTable` effect reads lines 423-428; the locked copy constants lines 28-48 — DO NOT reword)
    - .planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md (§ "src/ui/comparison-table.ts" lines 299-417 — exact import, effect-read, buildThead OFF/ON branches, buildBatteryRow conditional, ON-only disclaimer/note patterns with line refs)
    - tests/comparison-table.test.ts (full file — fixtures `makeSimResult`/`makeBattery` lines 30-56; setup lines 62-67; existing header-order, both-columns, leader, disclaimer-toggle, XSS tests as the pattern to adapt; imports line 22 must add `salderingOn`)
    - src/styles/comparison-table.css (lines 37-61 col-saldering / col-primary / col-muted / negative; lines 100-137 disclaimer + info-btn + negative-note — reuse existing classes)
  </read_first>
  <behavior>
    - With salderingOn false: thead has a single 'kWh netto-import vermeden' header for the avoided metric, NO 'zonder saldering'/'met saldering' sub-label cells; each row has exactly one avoided cell (avoidedOff)
    - With salderingOn true: thead shows the colspan-2 group header + zonder|met sub-labels (current Phase 4 layout); each row has both avoidedOff and avoidedOn cells
    - With salderingOn false: no #saldering-disclaimer element, no .saldering-info-btn, no .saldering-negative-note in the DOM
    - With salderingOn true and some avoidedOn <= 0: .saldering-negative-note present
    - Leader highlight (.table-cell--leader) applies to avoidedOff when OFF, and to the visible avoided columns when ON
    - The locked copy strings are unchanged byte-for-byte
  </behavior>
  <action>
    Add `salderingOn` to the import from '../state/app-state' (line 15). In `initComparisonTable`, read `const showSaldering = salderingOn.value` inside the effect (so the effect re-runs on toggle) and pass it to `renderTable(container, results, batteries, showSaldering)`.

    Thread a `showSaldering: boolean` parameter through `renderTable` → `buildThead` and `buildBatteryRow`. In `buildThead`: when `showSaldering` is true, keep the EXISTING colspan-2 group header (lines 113-127) and the zonder|met sub-label row entries (lines 166-176). When false, emit a single header `th` for the avoided metric with `rowspan='2'`, class `col-saldering-group__header`, textContent 'kWh netto-import vermeden', `dataset.metric = 'avoidedOff'`, and DO NOT emit the info button or the zonder/met sub-label cells. (The other rowspan='2' columns — Batterij, Zelfverbruik, Verschoven, Rest-import, Rest-teruglevering, Marginaal — are unchanged in both branches.)

    In `buildBatteryRow`: always append the avoidedOff cell (the post-2027 headline, D-07). Append the avoidedOn cell (lines 252-271, incl. the U+2212 negative formatting and .table-cell--negative logic — unchanged) ONLY when `showSaldering` is true.

    In `renderTable`: emit the disclaimer `<div id="saldering-disclaimer">` + call `wireSalderingDisclaimer(container)` ONLY when `showSaldering` is true (D-08) — reuse SALDERING_DISCLAIMER_COPY verbatim. Emit the negative-ON note ONLY when `showSaldering && hasNegativeOn` (change the existing condition from `hasNegativeOn` to `showSaldering && hasNegativeOn`). The cadence banner logic is independent of saldering — leave it unchanged. detectLeaders/deriveMetrics stay; leader detection already keys per-metric, so the leader class lands on whichever avoided cells are rendered.

    In tests/comparison-table.test.ts: add `salderingOn` to the imports (from '../src/state/signals' to stay worker-free). In setup/beforeEach reset `salderingOn.value = false`. Adapt the existing "both columns always present" test: it must now assert single-column when OFF and the pair when ON (set `salderingOn.value = true` and re-trigger). Add a `describe('saldering toggle (D-06..D-08)')` block covering all six <behavior> items, including: OFF → no #saldering-disclaimer / no .saldering-info-btn / no .saldering-negative-note; ON → pair + disclaimer present + negative-note when avoidedOn<=0; leader on avoidedOff in OFF mode. Keep the existing XSS test (script-named battery → zero <script>).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | head; npm test -- comparison-table 2>&1 | tail -25; grep -c "saldering is 100%" src/ui/comparison-table.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/comparison-table.ts` imports and reads `salderingOn` (assert: `grep -n "salderingOn" src/ui/comparison-table.ts`)
    - `renderTable`, `buildThead`, `buildBatteryRow` all accept/thread a `showSaldering` boolean (assert: `grep -n "showSaldering" src/ui/comparison-table.ts` shows 3+ signatures)
    - The locked copy is unchanged: `grep -c "saldering is 100%" src/ui/comparison-table.ts` returns 1 and the SALDERING_DISCLAIMER_COPY block is byte-identical to the pre-change version (no rewording)
    - `npm test -- comparison-table` exits 0
    - New tests assert: OFF single column (no zonder/met cells), ON pair, OFF has no #saldering-disclaimer and no .saldering-negative-note, ON+negative shows .saldering-negative-note
    - No inline styles added: `grep -n "\.style\." src/ui/comparison-table.ts` returns nothing
  </acceptance_criteria>
  <done>Table renders single avoided column when OFF, zonder|met pair when ON; disclaimer/info-btn/negative-note appear only when ON; leaders highlight visible columns; locked copy untouched; jsdom tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the saldering options-row toggle above the table in main.ts (survives re-renders) + CSS</name>
  <files>src/main.ts, src/styles/comparison-table.css</files>
  <read_first>
    - src/main.ts (full file — the #results-region wiring lines ~44-90; the `comparison-table-mount` pattern (a dedicated child div so renderTable's innerHTML='' does not clobber siblings); the period-control mount; the HMR dispose block)
    - .planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md (§ saldering options row lines 400-417 — must sit OUTSIDE the container renderTable clears; wire from the caller site in main.ts; checkbox writes salderingOn.value + scheduleRecompute(true))
    - src/state/app-state.ts (salderingOn + scheduleRecompute exports after 06-01)
    - src/styles/comparison-table.css (existing layout classes to stay consistent with; CSP note at top)
    - .planning/phases/06-CONTEXT.md (D-06 working label `Toon óók 'met saldering' (geldt t/m 2026)` — finalize consistent copy)
  </read_first>
  <action>
    In src/main.ts, inside the `if (resultsRegion)` block, BEFORE creating `comparison-table-mount`, build a saldering options row as a SIBLING element (appended to resultsRegion directly, not inside the comparison-table-mount that renderTable clears). Create a `<div class="saldering-options-row">` containing a `<label>` wrapping an `<input type="checkbox">` (default unchecked, matching `salderingOn` default false) and a text span with the finalized Dutch label — direction `Toon óók 'met saldering' (geldt t/m 2026)` (finalize wording consistent with the corrected disclaimer; keep it short). Optionally include an adjacent info (ⓘ) affordance per D-06. Set `checkbox.checked = salderingOn.value` for consistency. Wire the change handler: `salderingOn.value = checkbox.checked; scheduleRecompute(true)` (discrete event → immediate, per scheduleRecompute pattern). Append this options-row to resultsRegion, positioned above the comparison-table-mount. Add `salderingOn` and (if not already) `scheduleRecompute` to the imports from './state/app-state'. Do NOT place the checkbox inside comparison-table-mount (it would be wiped by container.innerHTML=''). Use textContent / static labels — no innerHTML, no inline styles.

    In src/styles/comparison-table.css add a `.saldering-options-row` rule (and label/checkbox spacing) using design tokens — no inline styles, no new hardcoded colors beyond tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | head; npm run build 2>&1 | tail -15; grep -n "saldering-options-row" src/main.ts src/styles/comparison-table.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.ts` creates a `.saldering-options-row` appended to `resultsRegion` as a sibling of (and above) `comparison-table-mount`, NOT as its child (assert: `grep -n "saldering-options-row" src/main.ts` and that it is appended to resultsRegion)
    - The checkbox change handler writes `salderingOn.value` and calls `scheduleRecompute(true)` (assert: `grep -n "salderingOn.value" src/main.ts` and `grep -n "scheduleRecompute(true)" src/main.ts`)
    - `src/styles/comparison-table.css` contains `.saldering-options-row`
    - `npm run build` succeeds (production bundle builds; confirms no CSP/inline-style violations from the build-only CSP plugin)
    - No inline styles: `grep -n "\.style\." src/main.ts` returns nothing new for the options row
  </acceptance_criteria>
  <done>A persistent options-row toggle sits above the table, default unchecked, writes salderingOn + triggers recompute, survives table re-renders, builds cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live human-verify — both behaviors through the real Comlink worker pipeline</name>
  <files>(no code changes — manual verification only)</files>
  <action>This is a human-verification checkpoint, not an implementation task. Do not edit code here. Run the app, present the verification steps in &lt;how-to-verify&gt; to the user, and pause for the resume-signal. See &lt;what-built&gt; for what was implemented across 06-02 and this plan.</action>
  <verify><human-check>User confirms every step in &lt;how-to-verify&gt; passes.</human-check></verify>
  <done>User types "approved" after both behavior sets (A multiple customs, B saldering toggle) verify correctly through the live worker pipeline.</done>
  <what-built>
    Multiple custom batteries (add/edit/remove, optional name, cap, order-based colors) from 06-02, AND the saldering toggle (single column OFF by default, zonder|met pair ON, ON-only disclaimer) from this plan. The full reactive pipeline (signal write → scheduleRecompute → Comlink worker → simResults → table/chart re-render) is NOT exercised in CI because Vitest mocks the Worker ([[test-worker-mock-blind-spot]]) — so this live check is the only verification that the round-trip actually re-renders correctly.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the served URL. Upload a real 15-minute P1 CSV (so simResults populate).

    A. Multiple custom batteries (06-02 / D-01..D-05):
      1. Click "+ Eigen batterij" twice. Confirm TWO fresh editable cards appear, named 'Eigen batterij 1' and 'Eigen batterij 2' by default.
      2. Fill capacity on both. Confirm two NEW columns appear in the comparison table within ~1s (the worker recomputed), each with a distinct swatch color matching its picker card, AND the same colors reused in the monthly-bars + flow charts (Phase 4/5 color contract).
      3. Overwrite one card's name to something custom; confirm the table column header updates to that name.
      4. Select catalog batteries until 5 valid batteries are active; confirm "+ Eigen batterij" disables and the cap note shows.
      5. Click "× Verwijderen" on the FIRST custom; confirm its column disappears, the slot frees (add re-enables), and the surviving custom's color reflows by order (shifts) consistently across table AND charts (D-05).

    B. Saldering toggle (this plan / D-06..D-08):
      6. On first load (before touching the toggle) confirm the comparison table shows a SINGLE 'kWh netto-import vermeden' column with NO 'zonder saldering'/'met saldering' sub-labels, and NO saldering disclaimer / info (i) button / negative note anywhere (D-07/D-08 default OFF).
      7. Check the "Toon óók 'met saldering'" toggle. Confirm the headline splits into the zonder | met pair within ~1s (worker recompute), the (i) info affordance + disclaimer become available, and — if any 'met saldering' value is ≤ 0 — the negative-ON note appears (D-07/D-08).
      8. Uncheck the toggle. Confirm it collapses back to the single column and all saldering verbiage disappears.
      9. Confirm zero console errors and zero network requests carrying CSV data throughout (privacy + CSP).
  </how-to-verify>
  <resume-signal>Type "approved" if all of A and B behave as described, or describe the specific step that failed.</resume-signal>
  <acceptance_criteria>
    - Adding/removing customs re-renders the table AND charts through the live worker (not just signal state)
    - Custom column header reflects the typed/default name
    - Cap disables add at 5 valid; remove frees the slot
    - Colors reflow by order on mid-list removal, consistent across table + charts
    - Default view is single-column with no saldering verbiage; toggle reveals pair + disclaimer; un-toggle hides them
    - No console errors; no CSV-bearing network requests; no CSP violations
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → DOM (custom battery NAME via column header) | The custom name (set in 06-02) is rendered into the table column header here via the existing `nameSpan.textContent = battery.name` (comparison-table.ts line 235). It stays textContent — no new innerHTML path is introduced. |
| user → saldering toggle | A boolean checkbox; no string crosses into the DOM. The disclaimer/note copy is static locked text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering/XSS (Injection) | custom name → table column header | mitigate | Header/cell name continues to use `.textContent` (existing line 235); no `innerHTML` added. Existing T-04-13 XSS test (script-named battery → zero <script>) retained and remains green. |
| T-06-06 | Tampering (CSP) | options-row + conditional layout | mitigate | Toggle/options-row + disclaimer gating use CSS classes + textContent only; no `.style.` assignments (D-10). `npm run build` exercises the build-only CSP plugin and must pass. |
| T-06-07 | Information Disclosure | live recompute pipeline | accept/verify | No user data leaves the device; the live human-verify step explicitly checks for zero CSV-bearing network requests (PRIV-01 carried forward). |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed; no install tasks in this plan. |
</threat_model>

<verification>
- `npm test -- comparison-table` green.
- `npm run build` succeeds (CSP plugin passes — no inline styles).
- `grep -c "saldering is 100%" src/ui/comparison-table.ts` == 1 (locked copy intact).
- Live human-verify confirms both behaviors through the real worker (covers the test blind spot).
</verification>

<success_criteria>
- Saldering OFF by default → single 'kWh netto-import vermeden' column, no saldering verbiage.
- Toggle ON → zonder|met pair + disclaimer + (conditionally) negative note.
- Options row persists across re-renders; toggle recomputes via existing worker.
- Both phase behaviors verified live (worker round-trip), no console errors, no CSV network calls.
</success_criteria>

<output>
Create `.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-03-SUMMARY.md` when done.
</output>
