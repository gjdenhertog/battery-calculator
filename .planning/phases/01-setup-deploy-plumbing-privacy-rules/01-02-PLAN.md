---
phase: 01-setup-deploy-plumbing-privacy-rules
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/csp-plugin.test.ts
  - tests/shell.test.ts
autonomous: true
requirements: [SETUP-05, PRIV-02]
must_haves:
  truths:
    - "A node-env test asserts the CSP string contains all required directives and NO 'unsafe-inline'/'unsafe-eval'"
    - "A jsdom test asserts the 3 shell regions render (header/drop-zone/results)"
    - "A jsdom test asserts the verbatim privacy promise renders inside the drop-zone region"
    - "A jsdom test asserts the app title is exactly 'Thuisbatterij Calculator'"
    - "npx vitest run is green and the suite fails if any contract regresses"
  artifacts:
    - path: "tests/csp-plugin.test.ts"
      provides: "CSP directive contract lock (node env)"
      contains: "default-src 'none'"
    - path: "tests/shell.test.ts"
      provides: "DOM-contract lock for the 3-region shell + privacy promise (jsdom)"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "tests/csp-plugin.test.ts"
      to: "src/constants/csp.ts"
      via: "import of CSP constant"
      pattern: "import.*CSP.*from.*constants/csp"
    - from: "tests/shell.test.ts"
      to: "src/shell.ts"
      via: "import of renderShell"
      pattern: "import.*renderShell.*from.*shell"
---

<objective>
Add the contract-locking Vitest suite (D-08): a node-env test that pins the CSP directive set, and a jsdom test that pins the 3-region shell DOM and the verbatim privacy promise. These tests make every Phase-1 visual/security contract regression-proof and become the test patterns Phases 2–5 inherit (node-default, jsdom-per-file via docblock — D-09).

Purpose: Without these, the CSP and shell contracts are review-dependent. With them, any future edit that drops a directive, weakens the CSP, removes a region, or alters the privacy copy fails CI.

Output: A green `npx vitest run` covering SETUP-05/D-08 (CSP) and PRIV-02/D-01 (shell + privacy promise).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-setup-deploy-plumbing-privacy-rules/01-CONTEXT.md
@.planning/phases/01-setup-deploy-plumbing-privacy-rules/01-UI-SPEC.md
@.planning/phases/01-setup-deploy-plumbing-privacy-rules/01-RESEARCH.md

<interfaces>
<!-- Contracts CONSUMED from Plan 01-01. Use these directly — no codebase exploration. -->

src/constants/csp.ts:
```typescript
export const CSP: string;  // '; '-joined directive string
```

src/shell.ts:
```typescript
export function renderShell(host: HTMLElement): void;  // builds .container > header[role=banner] / main#drop-zone-region / section#results-region
```

Shell DOM contract (from 01-UI-SPEC.md, locked):
- header[role="banner"] > h1 with textContent "Thuisbatterij Calculator"
- main#drop-zone-region > p.privacy-promise with EXACT text:
  "Je data blijft op je eigen apparaat — open je netwerktabblad en je ziet 0 verzoeken na het laden"  (em dash = U+2014)
- section#results-region[aria-label="Vergelijkingsresultaten"] — bare, no text (D-02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSP directive contract test (node env)</name>
  <files>tests/csp-plugin.test.ts</files>
  <behavior>
    - CSP contains "default-src 'none'"
    - CSP contains "script-src 'self'", "style-src 'self'", "img-src 'self'", "font-src 'self'"
    - CSP contains "connect-src 'none'"
    - CSP contains "base-uri 'self'", "form-action 'none'", "frame-ancestors 'none'"
    - CSP does NOT contain "'unsafe-inline'"
    - CSP does NOT contain "'unsafe-eval'"
  </behavior>
  <read_first>
    - src/constants/csp.ts (the CSP export created in Plan 01-01 Task 2)
    - .planning/phases/01-setup-deploy-plumbing-privacy-rules/01-RESEARCH.md §"Pattern 7" (csp-plugin.test.ts example — node env, no jsdom)
    - .planning/phases/01-setup-deploy-plumbing-privacy-rules/01-CONTEXT.md D-03 (full directive list), D-08
  </read_first>
  <action>
    Create tests/csp-plugin.test.ts running in the DEFAULT node environment (no jsdom docblock — this is a pure string assertion, fast). Import { CSP } from '../src/constants/csp'. Write a describe('CSP directive contract') with `it` cases asserting via expect(CSP).toContain(...) every directive listed in the behavior block (all 9 directives, D-03 order not required to assert but presence is), plus two negative assertions: expect(CSP).not.toContain("'unsafe-inline'") and expect(CSP).not.toContain("'unsafe-eval'"). This locks SETUP-05 / D-08: any future weakening of the policy fails the suite.
  </action>
  <verify>
    <automated>cd /Users/geraldjoachimdenhertog/projects/battery-calculator && npx vitest run tests/csp-plugin.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/csp-plugin.test.ts` exits 0 (all assertions pass).
    - The test imports CSP from '../src/constants/csp' (no inlined duplicate directive string).
    - The test includes both negative assertions for 'unsafe-inline' and 'unsafe-eval'.
    - `grep -q "@vitest-environment" tests/csp-plugin.test.ts` returns NON-zero (no jsdom docblock — runs in node default per D-09).
  </acceptance_criteria>
  <done>CSP directive set is locked by a green node-env test; weakening the policy or adding an unsafe-* keyword would fail the suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shell DOM-contract test (jsdom)</name>
  <files>tests/shell.test.ts</files>
  <behavior>
    - After renderShell(host): document contains header[role="banner"], #drop-zone-region, #results-region
    - h1 textContent equals "Thuisbatterij Calculator"
    - #drop-zone-region textContent contains the verbatim privacy promise (U+2014 em dash)
    - #results-region has aria-label "Vergelijkingsresultaten" and no privacy promise text (bare per D-02)
  </behavior>
  <read_first>
    - src/shell.ts (renderShell created in Plan 01-01 Task 3)
    - .planning/phases/01-setup-deploy-plumbing-privacy-rules/01-UI-SPEC.md §"Shell Structure Contract" (IDs, roles, exact title, verbatim privacy promise)
    - .planning/phases/01-setup-deploy-plumbing-privacy-rules/01-RESEARCH.md §"Pattern 7" (jsdom docblock usage, querySelector assertions)
    - .planning/phases/01-setup-deploy-plumbing-privacy-rules/01-CONTEXT.md D-01, D-02, D-08, D-09
  </read_first>
  <action>
    Create tests/shell.test.ts with the FIRST line `// @vitest-environment jsdom` (D-09 — per-file override, not global). Import { renderShell } from '../src/shell'. In a beforeEach, create a host element (e.g. document.body.innerHTML = '<div id="app"></div>') and call renderShell on it (or render into document.body) so the live jsdom DOM is populated by the real shell function — do NOT hand-write the shell HTML in the test (that would test the test, not the source). Then assert via querySelector: header[role="banner"] not null, #drop-zone-region not null, #results-region not null; h1.textContent === 'Thuisbatterij Calculator'; #drop-zone-region.textContent contains the verbatim "Je data blijft op je eigen apparaat — open je netwerktabblad en je ziet 0 verzoeken na het laden" (paste byte-for-byte with U+2014); #results-region.getAttribute('aria-label') === 'Vergelijkingsresultaten' and its textContent.trim() does NOT contain the privacy promise (D-02 bare placeholder). This locks PRIV-02 / D-01.
  </action>
  <verify>
    <automated>cd /Users/geraldjoachimdenhertog/projects/battery-calculator && npx vitest run tests/shell.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/shell.test.ts` exits 0.
    - First line of tests/shell.test.ts is `// @vitest-environment jsdom`.
    - The test imports renderShell from '../src/shell' and invokes it to populate the DOM (does not hand-author the shell markup).
    - Assertions cover all three regions, the exact h1 title, and the verbatim privacy promise inside #drop-zone-region.
    - `npx vitest run` (full suite) exits 0 with both test files green.
  </acceptance_criteria>
  <done>The 3-region shell, app title, and verbatim privacy promise are locked by a green jsdom test; removing a region or altering the privacy copy fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge | Tests run on every push (Plan 03 CI); a regressed contract must block the deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | silent CSP weakening in a future edit | mitigate | tests/csp-plugin.test.ts asserts full directive set + negative unsafe-* checks; runs in CI on every push |
| T-01-02-02 | Tampering | silent removal of the privacy promise / a shell region | mitigate | tests/shell.test.ts asserts verbatim copy + all 3 regions via the real renderShell output |
| T-01-02-SC | Tampering | jsdom test dependency | accept | jsdom `[ASSUMED]` per RESEARCH audit — ~14yr history, 30M+/wk; no blocking checkpoint needed |
</threat_model>

<verification>
- `npx vitest run` is green with both test files.
- Manually weakening CSP (e.g. removing connect-src) or altering the privacy string would turn the suite red (regression guard intact).
</verification>

<success_criteria>
- CSP directive contract locked (SETUP-05, D-08; ROADMAP criterion 3).
- Shell + verbatim privacy promise locked (PRIV-02, D-01; ROADMAP criterion 5).
- Test patterns (node default, jsdom-per-file) established for Phases 2–5 (D-09).
</success_criteria>

<output>
Create `.planning/phases/01-setup-deploy-plumbing-privacy-rules/01-02-SUMMARY.md` when done.
</output>
