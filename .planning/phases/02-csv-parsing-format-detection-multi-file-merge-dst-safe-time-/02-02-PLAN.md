---
phase: 02-csv-parsing-format-detection-multi-file-merge-dst-safe-time
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/domain/encoding.ts
  - src/domain/parsers/homewizard-p1.ts
  - src/domain/parsers/noop-stub.ts
  - src/domain/parse.ts
  - tests/homewizard-p1.test.ts
  - tests/encoding.test.ts
  - tests/parse-errors.test.ts
  - tests/dst-fixtures.test.ts
autonomous: true
requirements: [DATA-02, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09, DATA-13]

must_haves:
  truths:
    - "A HomeWizard P1 CSV (auto-detected by header) parses to IntervalSample[] with gridImportKwh >= 0 and gridExportKwh >= 0"
    - "The series is classified cumulative; per-interval deltas are computed from successive rows; the first row is consumed as a reference (firstIntervalAnomalyFlag true)"
    - "A negative delta sets isMonotonic false and records the failing row (meter-swap / wrong-column surfacing)"
    - "Timestamps are UTC Date instances converted from Europe/Amsterdam local time via TZDate"
    - "The 2026-03-29 spring fixture yields exactly 92 intervals; the 2026-10-25 fall fixture yields exactly 100"
    - "UTF-8 (with/without BOM), Windows-1252 fallback, ; delimiter, and decimal comma all parse; encoding used is reported"
    - "A malformed row aborts the file parse and throws ParseRowError with file/row/column/expected"
  artifacts:
    - path: "src/domain/parsers/homewizard-p1.ts"
      provides: "HomeWizard P1 adapter (cumulative→delta, TZDate, self-registering)"
      contains: "ParserRegistry.register"
    - path: "src/domain/parse.ts"
      provides: "parseFile() — encoding detect + PapaParse worker:true + registry dispatch"
      contains: "worker: true"
    - path: "src/domain/encoding.ts"
      provides: "decodeFileWithFallback() — UTF-8 then Windows-1252"
      contains: "TextDecoder"
    - path: "src/domain/parsers/noop-stub.ts"
      provides: "Second registry parser proving DATA-03 with zero central-switch edits"
  key_links:
    - from: "src/domain/parsers/homewizard-p1.ts"
      to: "src/domain/parsers/registry.ts"
      via: "ParserRegistry.register on import"
      pattern: "ParserRegistry\\.register"
    - from: "src/domain/parse.ts"
      to: "src/domain/parsers/homewizard-p1.ts"
      via: "side-effect import"
      pattern: "import './parsers/homewizard-p1'"
---

<objective>
Build the parsing pipeline: encoding detection with UTF-8→Windows-1252 fallback, the
HomeWizard P1 adapter (cumulative→delta, DST-safe TZDate timestamps, self-registering),
a noop second parser proving the registry, and the `parseFile()` orchestrator using
PapaParse `worker: true` with auto-detect delimiter and fail-fast error handling.

Purpose: This is the concrete data-ingestion path — DATA-02/04/05/06/07/08/09/13. It turns
a real HomeWizard CSV into the canonical `IntervalSample[]` Phase 3 will simulate over.

Output: `src/domain/encoding.ts`, `src/domain/parsers/homewizard-p1.ts`,
`src/domain/parsers/noop-stub.ts`, `src/domain/parse.ts`, and four node-env test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md

<interfaces>
<!-- This plan CONSUMES the contract created in Plan 02-01. Import these directly; do not re-derive. -->
From src/domain/types.ts:
  IntervalSample { timestamp: Date; gridImportKwh: number; gridExportKwh: number }
  SeriesType = 'cumulative' | 'interval'
  ParseFileResult { fileName; encoding; seriesType; cadenceMinutes; samples; rowCount; isMonotonic; monotonicity_failRow?; firstIntervalAnomalyFlag; softWarnings? }
  class ParseRowError(fileName,rowNumber,columnName,expected,actual)
  class UnsupportedEncodingError(fileName)
From src/domain/parsers/registry.ts:
  interface CsvParser { name; claim(headers): boolean; transform(rows, file): ParseFileResult }
  const ParserRegistry { register(p); claim(headers) }
</interfaces>

<!-- Full code sketches (do NOT inline in actions): -->
<!-- Adapter: 02-RESEARCH.md §"HomeWizard P1 Adapter Sketch" + §"Cumulative-to-Delta Conversion" -->
<!-- Timestamp: 02-RESEARCH.md §"DST-Safe Bucketing with TZDate" (Pattern 2) -->
<!-- Encoding: 02-RESEARCH.md §"Pattern 4: Encoding Detection" -->
<!-- parse.ts: 02-RESEARCH.md §"PapaParse with Worker and Encoding" -->
<!-- noop-stub: 02-PATTERNS.md §"src/domain/parsers/noop-stub.ts" -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encoding detection (UTF-8 → Windows-1252 fallback)</name>
  <files>src/domain/encoding.ts, tests/encoding.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"Pattern 4: Encoding Detection" (decodeFileWithFallback sketch; BOM note) and §"Pitfall 7" (Windows-1252 ICU caveat)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md D-07 (encoding policy)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"Node-environment test files" (test header pattern; File via `new File([str], name, {type})`)
    - src/domain/types.ts (UnsupportedEncodingError)
  </read_first>
  <behavior>
    - A UTF-8 string (no BOM) decodes and reports encoding 'UTF-8'.
    - A UTF-8-with-BOM buffer decodes with the BOM stripped from the result and reports 'UTF-8'.
    - A buffer with a byte invalid under UTF-8 but valid under Windows-1252 (e.g. 0xE9 alone) decodes via the fallback and reports 'Windows-1252'.
    - A buffer that decodes under neither throws UnsupportedEncodingError naming the file.
  </behavior>
  <action>
    Create `src/domain/encoding.ts` exporting `async decodeFileWithFallback(file: File): Promise<{ text: string; encoding: 'UTF-8' | 'Windows-1252' }>` per RESEARCH.md §Pattern 4: read `file.arrayBuffer()`, try `new TextDecoder('utf-8', { fatal: true })` (BOM auto-stripped), on throw fall back to `new TextDecoder('windows-1252')`, and throw `UnsupportedEncodingError(file.name)` if neither yields. Write `tests/encoding.test.ts` (node env, no jsdom docblock) constructing `File`/`Blob` inputs from `Uint8Array` to cover the four behaviors. If `TextDecoder('windows-1252')` is unavailable in the node test env (Pitfall 7), guard that one case with a capability check and record the limitation in the SUMMARY rather than failing the suite.
  </action>
  <verify>
    <automated>npm test -- tests/encoding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/encoding.test.ts` exits 0 (DATA-04, D-07).
    - UTF-8-with-BOM test asserts the decoded string does NOT start with the BOM codepoint U+FEFF (charCodeAt(0) !== 0xFEFF).
    - Fallback test asserts returned `encoding === 'Windows-1252'`.
    - Failure test asserts `UnsupportedEncodingError` is thrown with the file name in the message.
  </acceptance_criteria>
  <done>decodeFileWithFallback handles UTF-8/BOM, Windows-1252 fallback, and unsupported-encoding failure; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeWizard P1 adapter + noop stub (cumulative→delta, DST-safe, self-registering)</name>
  <files>src/domain/parsers/homewizard-p1.ts, src/domain/parsers/noop-stub.ts, tests/homewizard-p1.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"HomeWizard P1 Adapter Sketch", §"Cumulative-to-Delta Conversion", §"Pattern 2: DST-Safe Bucketing with TZDate", §"Pitfall 3" (first-row delta), §"Pitfall 6" (decimal comma)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/parsers/homewizard-p1.ts" and §"src/domain/parsers/noop-stub.ts" (side-effect register + `export {}`)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md D-05, D-06 (flag-not-fabricate vs fail-fast)
    - src/domain/types.ts, src/domain/parsers/registry.ts
  </read_first>
  <behavior>
    - claim(['time','Import T1 kWh','Import T2 kWh','Export T1 kWh','Export T2 kWh']) returns true; claim of unrelated headers returns false (DATA-02).
    - transform of cumulative rows produces samples where each gridImportKwh = (curr T1+T2 import) − (prev T1+T2 import), clamped to >= 0; same for export (DATA-05, DATA-06).
    - The first raw row is consumed as reference only (samples.length === rawRows − 1) and firstIntervalAnomalyFlag is true (DATA-05, Pitfall 3).
    - A row whose cumulative value decreases sets isMonotonic false and records monotonicity_failRow (DATA-05).
    - A value with a decimal comma ("8354,590") parses correctly (DATA-04, Pitfall 6).
    - timestamp values are UTC Date: a known Amsterdam-winter local "2026-01-15 12:00" maps to the correct UTC ms (DATA-07).
    - noop-stub: claim() always false; registering it does not break HomeWizard claim resolution (DATA-03).
  </behavior>
  <action>
    Create `src/domain/parsers/homewizard-p1.ts` implementing the `CsvParser` per RESEARCH.md §Adapter Sketch: `claim()` checks the required headers; `transform()` parses each cell with a decimal-comma-normalizing `parseKwh` (throws `ParseRowError` on non-numeric per D-06), parses the `time` column via a `parseLocalTimestamp` that uses `new TZDate(yr, mo-1, dy, hr, mn, 'Europe/Amsterdam')` then `.getTime()` to a UTC `Date` (never native `new Date(string)`), computes per-interval deltas from index 1 (discarding row[0], setting `firstIntervalAnomalyFlag: true`), clamps each delta to `Math.max(0, delta)` AFTER flagging `isMonotonic`/`monotonicity_failRow` on negative deltas (D-05 flag-not-fabricate), sets `seriesType: 'cumulative'` and `cadenceMinutes` via median interval. End the file with `ParserRegistry.register(HomeWizardP1Parser)` and `export {}`. Create `src/domain/parsers/noop-stub.ts` per 02-PATTERNS.md (claim always false, transform throws, self-registers, `export {}`). Write `tests/homewizard-p1.test.ts` (node env) covering all behaviors above with inline row fixtures.
  </action>
  <verify>
    <automated>npm test -- tests/homewizard-p1.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/homewizard-p1.test.ts` exits 0 (DATA-02/05/06/07).
    - All samples assert `gridImportKwh >= 0 && gridExportKwh >= 0` (DATA-06).
    - Test asserts `samples.length === rawRows - 1` and `firstIntervalAnomalyFlag === true` (DATA-05).
    - Decimal-comma test asserts the parsed delta equals the dot-decimal equivalent (DATA-04).
    - `grep -c "export {}" src/domain/parsers/homewizard-p1.ts` returns 1 and same for noop-stub.ts (tree-shake guard, Pitfall 5).
    - `grep -c "new Date(" src/domain/parsers/homewizard-p1.ts` — every occurrence wraps a `TZDate(...).getTime()`, none parses a raw string (DATA-07).
  </acceptance_criteria>
  <done>HomeWizard adapter converts cumulative→delta with UTC timestamps, flags monotonicity/first-interval, self-registers; noop stub proves the registry; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parseFile() orchestrator + parse-error + DST fixture validation</name>
  <files>src/domain/parse.ts, tests/parse-errors.test.ts, tests/dst-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"PapaParse with Worker and Encoding" (parse.ts sketch; step fail-fast; complete dispatch) and §"DST-Safe Bucketing" (92/100 math) and §"Pitfall 5" (static side-effect imports)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/parse.ts"
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-VALIDATION.md (DATA-08, DATA-09 test rows)
    - tests/fixtures/homewizard-spring-2026-03-29.csv, tests/fixtures/homewizard-fall-2026-10-25.csv (created in Plan 02-01)
    - src/domain/encoding.ts, src/domain/parsers/registry.ts
  </read_first>
  <behavior>
    - parseFile of a HomeWizard CSV resolves to a ParseFileResult whose samples are non-empty and whose encoding is reported.
    - parseFile of a CSV with a non-numeric kWh cell rejects with ParseRowError naming file, row, column, expected (DATA-09).
    - parseFile of a CSV whose headers match no parser rejects with an "Onbekend bestandsformaat" error naming the file.
    - The spring fixture (2026-03-29) yields exactly 92 IntervalSamples (DATA-08).
    - The fall fixture (2026-10-25) yields exactly 100 IntervalSamples (DATA-08).
  </behavior>
  <action>
    Create `src/domain/parse.ts` exporting `async parseFile(file: File): Promise<ParseFileResult>` per RESEARCH.md §"PapaParse with Worker and Encoding": STATIC side-effect imports of `./parsers/homewizard-p1` and `./parsers/noop-stub` (the one-file-change registration point — Pitfall 5), call `decodeFileWithFallback`, run `Papa.parse(text, { header: true, delimiter: '', dynamicTyping: false, worker: true, skipEmptyLines: true, step, complete, error })`; in `step` fail-fast on the first row error by aborting and rejecting with `ParseRowError`; in `complete` call `ParserRegistry.claim(headers)`, reject with the "Onbekend bestandsformaat voor \"{file}\"" error if null, else `resolve({ ...adapter.transform(rows, file), encoding })`. Write `tests/parse-errors.test.ts` (node env) for the malformed-row and unknown-format rejections, and `tests/dst-fixtures.test.ts` (node env) that loads the two fixture CSVs from `tests/fixtures/`, wraps them as `File` objects, runs them through the adapter transform path, and asserts sample counts of exactly 92 and 100. NOTE: PapaParse `worker: true` is for the browser path; in node tests, call the adapter `transform` directly on parsed rows (or use `worker: false` in the test harness) so the DST/error assertions run deterministically without a worker — keep `worker: true` in the production `parse.ts` config for DATA-13.
  </action>
  <verify>
    <automated>npm test -- tests/parse-errors.test.ts tests/dst-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dst-fixtures.test.ts` asserts the 2026-03-29 fixture produces exactly 92 samples and 2026-10-25 produces exactly 100 (DATA-08); test exits 0.
    - `tests/parse-errors.test.ts` asserts a thrown ParseRowError exposes `.fileName`, `.rowNumber`, `.columnName`, `.expected` (DATA-09); test exits 0.
    - `grep -c "worker: true" src/domain/parse.ts` returns 1 (DATA-13 production path).
    - `grep -c "import './parsers/homewizard-p1'" src/domain/parse.ts` returns 1 (DATA-02/03 static registration).
    - Full `npm test` exits 0.
  </acceptance_criteria>
  <done>parseFile orchestrates decode→PapaParse(worker:true)→registry dispatch with fail-fast; DST fixtures verified at 92/100; parse errors carry full DATA-09 fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded CSV file → parser | Untrusted file content (cell values, file name, encoding) enters the domain layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | CSV formula/script in cells (e.g. `=CMD()`) | mitigate | Cells only ever `parseFloat`'d or regex-matched as timestamps; never `eval`/`Function`; never rendered as HTML in this layer. |
| T-02-04 | Tampering | Prototype pollution via CSV header keys | mitigate | PapaParse `header:true` yields plain objects; adapter reads only the known fixed header names; no dynamic property writes from CSV keys. |
| T-02-05 | Denial of Service | very large file freezing UI | mitigate | PapaParse `worker: true` + streaming `step` keeps parse off the main thread (DATA-13). |
| T-02-06 | Information Disclosure | CSV data leaving the device | accept→mitigate | Domain layer makes no `fetch`/XHR; `connect-src 'none'` (from Plan 01) blocks exfiltration. |
</threat_model>

<verification>
- `npm test` full suite green (encoding, homewizard-p1, parse-errors, dst-fixtures).
- DST fixtures assert exactly 92 / 100 intervals.
- `npx tsc --noEmit` exits 0.
- Adapter + noop-stub both end with `export {}`; parse.ts statically imports both.
</verification>

<success_criteria>
- DATA-02: HomeWizard CSV auto-detected via registry → IntervalSample[].
- DATA-04: UTF-8/BOM, Windows-1252, `;` delimiter, decimal comma all handled.
- DATA-05/06: cumulative→delta, monotonicity flag, first-interval flag, non-negative two-field energy.
- DATA-07: UTC timestamps via TZDate.
- DATA-08: 92 / 100 DST interval counts proven by fixture tests.
- DATA-09: ParseRowError with file/row/column/expected.
- DATA-13: parse.ts production config uses `worker: true`.
</success_criteria>

<output>
Create `.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-02-SUMMARY.md` when done.
</output>
