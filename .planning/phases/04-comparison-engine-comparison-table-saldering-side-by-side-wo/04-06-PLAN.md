---
phase: 04-comparison-engine-comparison-table-saldering-side-by-side-wo
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-04, 04-05]
files_modified:
  - src/main.ts
  - src/styles/global.css
autonomous: false
requirements: [SIM-07, SIM-08, COMP-04, COMP-08]

must_haves:
  truths:
    - "On load, the battery picker mounts in #drop-zone-region with Sessy 5 pre-selected, before any upload"
    - "After uploading a CSV, the period control + comparison table fill #results-region"
    - "The production build emits the Comlink sim-worker as a chunk under dist/assets/"
    - "The UI stays interactive while the worker computes (Rekenen indicator visible, controls not locked)"
    - "Per-battery color is consistent between the picker swatch and the table row"
  artifacts:
    - path: "src/main.ts"
      provides: "Full Phase 4 init wiring: picker + period control + comparison table"
      contains: "initBatteryPicker"
    - path: "src/styles/global.css"
      provides: "Imports of the 3 new Phase 4 CSS files"
  key_links:
    - from: "src/main.ts"
      to: "src/ui/battery-picker.ts + period-control.ts + comparison-table.ts"
      via: "init calls against #drop-zone-region and #results-region"
      pattern: "initBatteryPicker|initPeriodControl|initComparisonTable"
    - from: "src/styles/global.css"
      to: "battery-picker.css + comparison-table.css + results-region.css"
      via: "@import"
      pattern: "@import"
---

<objective>
Wire the full Phase 4 UI together in `src/main.ts` (D-16): mount the battery picker inside
`#drop-zone-region` alongside the re-wired drop-zone, and fill `#results-region` with the
period control + comparison table. Import the three new CSS files. Then run the production
build to re-assert the Comlink worker chunk emits under `dist/assets/` (the HIGH-risk A3
assumption now that `new SimWorker()` is actually wired), and perform the live human-verify
of the end-to-end flow + the two manual-only checks from the validation contract.

Purpose: This is the integration seam where the proven pieces become one working UI — the
phase's first real interactive surface. The worker-chunk emission and "UI stays interactive
during compute" (SIM-08) and "color consistent picker↔table↔Phase-5" (COMP-04/08) can only be
confirmed in a running browser, hence the blocking human-verify checkpoint.
Output: wired main.ts, global.css imports, verified build + live walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-PATTERNS.md
@.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-UI-SPEC.md

<interfaces>
From src/ui/battery-picker.ts (04-04): `initBatteryPicker(region)`
From src/ui/period-control.ts (04-05): `initPeriodControl(container)`
From src/ui/comparison-table.ts (04-05): `initComparisonTable(container)`
From src/ui/drop-zone.ts (04-03): `initDropZone(region)` (already wired in main.ts)

<!-- Current src/main.ts (PATTERNS §main.ts): renders shell, then initDropZone(dropZoneRegion).
     #drop-zone-region and #results-region come from the Phase 1 shell. Keep null-guards. -->
<!-- src/styles/global.css currently imports drop-zone.css; add the 3 new files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire main.ts + global.css imports + re-assert worker chunk build</name>
  <files>src/main.ts, src/styles/global.css</files>
  <read_first>
    - src/main.ts (full file: existing render-shell + initDropZone null-guard pattern to extend)
    - src/styles/global.css (existing @import of drop-zone.css to mirror)
    - .planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-PATTERNS.md §"src/main.ts" (the exact extension snippet)
    - .planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-RESEARCH.md §"Open Questions" Q1, §"Assumptions Log" A3
  </read_first>
  <action>
    In `src/main.ts`, after the existing `initDropZone(dropZoneRegion)` call, add (with the same null-guard style): `import { initBatteryPicker } from './ui/battery-picker'`, `initPeriodControl`, `initComparisonTable`, and import the 3 new CSS files (or rely on global.css imports — choose one consistent approach and document it). Mount `initBatteryPicker(dropZoneRegion)` inside `#drop-zone-region` (D-16 — picker shares the region with the drop-zone; the verbatim privacy-promise stays). Resolve `#results-region` and, when present, call `initPeriodControl(resultsRegion)` then `initComparisonTable(resultsRegion)`. Importing app-state (transitively via the UI modules) self-initializes the worker singleton once.
    In `src/styles/global.css`, add `@import` lines for `battery-picker.css`, `comparison-table.css`, `results-region.css` next to the existing `drop-zone.css` import (or import them from main.ts — keep it consistent with however drop-zone.css is currently wired).
    Run the production build (`npm run build`) and inspect `dist/assets/` for the emitted Comlink worker chunk — confirm a separate JS file corresponding to `sim-worker` exists (e.g. `ls dist/assets | grep -i "sim-worker\|worker"`). This re-asserts A3 now that `new SimWorker()` is actually reached from the import graph (it was only present-but-unimported in 04-01). If the chunk is absent, the worker is not being pulled in — fix the import path before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm run build && ls dist/assets | grep -iE "worker" && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.ts` contains `initBatteryPicker`, `initPeriodControl`, and `initComparisonTable` calls with null-guards
    - `dist/assets/` after `npm run build` contains a JS chunk whose name matches `worker` (the Comlink sim-worker chunk — A3 confirmed)
    - `npm test -- --run` (full suite) exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>main.ts wires picker + period control + table; CSS imported; production build emits the worker chunk under dist/assets/; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verify end-to-end flow + interactivity + color consistency</name>
  <action>Human-verify checkpoint (no code). Build + preview the app and walk through the verification steps below; pause for the user to confirm zero CSP/network violations, the correct OFF-led saldering table, an interactive compute, and consistent per-battery color before resuming.</action>
  <what-built>
    Full Phase 4 interactive UI: a battery spec-card picker (Sessy 5 pre-selected) in the
    upload region, a period-narrowing control + comparison table in the results region. The
    table shows "kWh netto-import vermeden" zonder/met saldering side-by-side, per-column
    leader highlights, a coarse-cadence banner for daily data, and a "Rekenen..." indicator
    that keeps the UI responsive while the Comlink worker computes. Color is assigned per
    battery by selection order (picker swatch == table row swatch), preserved for Phase 5.
  </what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` and open the previewed URL (note the
       `/battery-calculator/` base path).
    2. Open DevTools Console + Network tabs. Confirm ZERO CSP violations on load and zero
       third-party / network requests (privacy invariant; `connect-src 'none'`).
    3. On load: the battery picker shows with Sessy 5 pre-checked, BEFORE any upload.
    4. Upload `tests/fixtures/homewizard-real-sample-15-minutes.csv`. Confirm the period
       control + comparison table appear in the results region, and "{N} dagen aan data"
       shows (no "/jaar" or "/maand" anywhere).
    5. Confirm the table headline columns are "kWh netto-import vermeden — zonder saldering"
       (primary) then "met saldering" (muted); a per-column leader cell is bolded/tinted; if
       any "met saldering" value is ≤ 0 it shows in red AS-IS (not floored). Click the "i"
       button → the saldering disclaimer expands.
    6. Upload the daily-granularity fixture `tests/fixtures/P1e-2025-1-1-2025-12-31.csv` (or
       the owner's daily export) → confirm the coarse-cadence warning banner appears ABOVE
       the table.
    7. INTERACTIVITY (SIM-08): select/deselect several batteries rapidly and drag the date
       inputs while results recompute — confirm the "Rekenen..." indicator shows, the prior
       table dims but stays visible, and the controls never lock / no obvious jank > 200ms.
    8. COLOR (COMP-04/08): confirm each battery's swatch color in the picker matches its row
       swatch color in the table, and that color follows selection order.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (CSP violation, missing column, floored negative, frozen UI, color mismatch, missing banner).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production build → browser CSP | the emitted worker chunk URL must satisfy worker-src 'self' |
| running app → network | the privacy invariant: zero requests with user data after load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | network egress of user CSV in the live app | mitigate | Human-verify step 2 confirms zero network requests + zero CSP violations; `connect-src 'none'` enforced; Comlink uses postMessage only |
| T-04-18 | Tampering | CSP violation blocking the worker in production (A3 risk) | mitigate | Build step inspects dist/assets/ for the worker chunk; human-verify step 2 confirms no `worker-src` CSP violation in the console |
| T-04-19 | Denial of Service | UI lock-up during compute defeating interactivity | mitigate | Human-verify step 7 exercises rapid toggling + date drag; worker offload + generation guard keep the main thread responsive |
</threat_model>

<verification>
- `npm run build` exits 0 and `ls dist/assets | grep -iE worker` matches a chunk
- `npm test -- --run` (full suite) exits 0
- Human-verify checkpoint: zero CSP violations, zero network requests, correct table, interactive UI, consistent color
</verification>

<success_criteria>
- Picker mounts on load (Sessy 5 pre-selected); table + period control fill results region after upload
- Worker chunk emitted under dist/assets/ (A3 confirmed)
- Live: zero CSP/network violations, OFF-led saldering table, leaders, un-floored negative ON, cadence banner, interactive compute, consistent per-battery color
</success_criteria>

<output>
Create `.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-06-SUMMARY.md` when done
</output>
