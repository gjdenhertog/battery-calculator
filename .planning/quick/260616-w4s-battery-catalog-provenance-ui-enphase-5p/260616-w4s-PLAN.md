---
phase: quick-260616-w4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/battery-catalog.ts
  - src/ui/battery-picker.ts
  - src/styles/battery-picker.css
  - tests/catalog.test.ts
  - tests/battery-picker.test.ts
autonomous: true
requirements: [BATT-01, BATT-02]
must_haves:
  truths:
    - "Catalog ships 8 entries including the Enphase IQ Battery 5P (datasheet-verified specs)"
    - "Every catalog entry declares which spec fields are assumed via assumedFields"
    - "A 'geschat' badge renders next to each spec row whose field is in the battery's assumedFields"
    - "Each battery card shows a '📄 Datasheet' link to datasheetUrl that opens in a new tab"
    - "The datasheet link is XSS/CSP-safe (createElement + setAttribute, no innerHTML) and does NOT trip the privacy guard"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "assumedFields?: ReadonlyArray<keyof spec fields> on BatteryConfig"
      contains: "assumedFields"
    - path: "src/domain/battery-catalog.ts"
      provides: "8 entries with assumedFields populated per RESEARCH map + enphase-5p entry"
      contains: "enphase-5p"
    - path: "src/ui/battery-picker.ts"
      provides: "geschat badge per assumed spec row + Datasheet link per card"
      contains: "geschat"
  key_links:
    - from: "src/ui/battery-picker.ts"
      to: "battery.assumedFields"
      via: "appendSpec passes isAssumed; badge rendered when field ∈ assumedFields"
      pattern: "assumedFields"
    - from: "src/ui/battery-picker.ts"
      to: "battery.datasheetUrl"
      via: "createElement('a') + setAttribute(href/target/rel) + textContent"
      pattern: "datasheetUrl"
---

<objective>
Add customer-facing spec provenance to the battery picker and a new Enphase IQ Battery 5P catalog entry (post-v1.0 / v1.1 work).

Two user-visible changes:
1. A small Dutch **"geschat"** (estimated) badge appears next to each spec row whose value is an assumption rather than a datasheet-cited figure.
2. A **"📄 Datasheet"** link per battery card opens the vendor datasheet in a new tab.

Plus one data change: add the **Enphase IQ Battery 5P** (id `enphase-5p`) to the catalog with datasheet-verified specs.

Purpose: honest provenance lets a non-engineer see at a glance which numbers are vendor-cited vs. our defensible v1 assumptions, and gives them a one-click path to the source datasheet.

Output: updated domain type, catalog data, picker UI + CSS, and tests; full local CI green and the deploy.yml privacy guard still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260616-w4s-battery-catalog-provenance-ui-enphase-5p/260616-w4s-RESEARCH.md

<!-- RESEARCH.md is orchestrator-VERIFIED ground truth: the Enphase specs and the per-battery -->
<!-- assumedFields provenance map are already pinned. Use those EXACT values. Do NOT re-derive. -->

@src/domain/types.ts
@src/domain/battery-catalog.ts
@src/ui/battery-picker.ts
@src/styles/battery-picker.css
@tests/catalog.test.ts
@tests/battery-picker.test.ts

<interfaces>
<!-- Key contracts the executor needs — no codebase exploration required. -->

BatteryConfig spec-field keys (the five physics fields that can be assumed):
  nominalCapacityKwh | dodFraction | roundTripEfficiency | maxChargeKw | maxDischargeKw

Spec-row label → field mapping (from buildSpecCard appendSpec calls in src/ui/battery-picker.ts):
  'Capaciteit'   → nominalCapacityKwh
  'Bruikbaar'    → dodFraction
  'Rendement'    → roundTripEfficiency
  'Max laden'    → maxChargeKw
  'Max ontladen' → maxDischargeKw

Current appendSpec signature (XSS-safe; value via textContent):
  function appendSpec(dl: HTMLElement, label: string, value: string): void

Catalog is a `readonly BatteryConfig[]` exported as BATTERY_CATALOG; Sessy 5 MUST remain index 0 (BATT-03).

Custom batteries are built in buildCustomCard() and committed via buildPartialOrNull() — they
construct a BatteryConfig WITHOUT assumedFields. Keeping assumedFields OPTIONAL means custom
batteries (all user-provided) correctly default to "no assumed fields" with zero changes there.

Existing test fixtures across tests/ (compare, app-state, sim-worker, comparison-table, simulate)
build BatteryConfig literals WITHOUT assumedFields. assumedFields MUST be OPTIONAL so these
literals keep type-checking unchanged.
</interfaces>

<assumedFields_map>
<!-- Ground truth from RESEARCH.md — copy these EXACTLY. -->
sessy-5         → []  (all cited)
sessy-10        → ['roundTripEfficiency','maxChargeKw','maxDischargeKw']
zonneplan-10    → ['dodFraction','roundTripEfficiency']
powerwall-3     → []  (all cited)
huawei-luna-5   → ['roundTripEfficiency']
victron-ess-10  → ['dodFraction','roundTripEfficiency','maxChargeKw','maxDischargeKw']
marstek-venus-e → ['dodFraction']
enphase-5p      → ['maxChargeKw']

Enphase IQ Battery 5P entry (id `enphase-5p`, name "Enphase IQ Battery 5P"):
  nominalCapacityKwh: 5.0
  dodFraction: 1.0
  roundTripEfficiency: 0.9
  maxChargeKw: 3.2
  maxDischargeKw: 3.2
  datasheetUrl: 'https://enphase.com/en-gb/download/iq-battery-5p-data-sheet'
  assumedFields: ['maxChargeKw']
</assumedFields_map>

<privacy_guard_note>
The deploy.yml privacy guard greps dist/ for external URLs but pipes through `grep -v '\.map'`,
which drops the minified bundle line that already contains all 7 catalog datasheet URLs — that is
why the guard passes today. The new Enphase URL is stored the exact same way (a string literal in
the bundled catalog) and renders into the DOM only at runtime as an <a href>, so it never appears
in static index.html. Do NOT alter the guard. The plan's final verify re-runs build + the guard
grep to confirm the 8th URL changes nothing.
</privacy_guard_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assumedFields to BatteryConfig + populate all 8 catalog entries (incl. Enphase 5P)</name>
  <files>src/domain/types.ts, src/domain/battery-catalog.ts, tests/catalog.test.ts</files>
  <action>
In src/domain/types.ts, add an OPTIONAL field to the BatteryConfig interface:
`assumedFields?: ReadonlyArray<'nominalCapacityKwh' | 'dodFraction' | 'roundTripEfficiency' | 'maxChargeKw' | 'maxDischargeKw'>`.
Document it: lists which of the five physics fields are [ASSUMED] v1 defaults rather than
datasheet-cited (BATT-01). Omitted/empty ⇒ all values cited or user-provided (custom batteries).
MUST be optional so existing test fixtures and the custom-battery builder keep type-checking.

In src/domain/battery-catalog.ts, add `assumedFields: [...]` to each of the 7 existing entries
using the EXACT arrays from <assumedFields_map> above (sessy-5 and powerwall-3 get `[]`). Then
append the new 8th entry `enphase-5p` with the exact specs from <assumedFields_map>. Keep Sessy 5
at index 0 (BATT-03). Update the file's docblock to note assumedFields is the machine-readable
mirror of the prose [ASSUMED] comments. Do NOT change any existing numeric spec value.

In tests/catalog.test.ts, add assertions (do not remove existing ones — they already use
BATTERY_CATALOG.length dynamically and tolerate 8 entries via the 6–8 bound):
(a) an entry with id `enphase-5p` exists with name "Enphase IQ Battery 5P" and the exact specs
    (nominalCapacityKwh 5.0, dodFraction 1.0, roundTripEfficiency 0.9, maxChargeKw 3.2,
    maxDischargeKw 3.2, the enphase datasheet URL);
(b) every entry's `assumedFields` (when present) is an array whose members are all valid spec-field
    keys (subset of the five) — shape lock;
(c) the enphase-5p entry's assumedFields deep-equals ['maxChargeKw'].
  </action>
  <verify>
    <automated>TZ=Europe/Amsterdam npx vitest run tests/catalog.test.ts && npm run typecheck</automated>
  </verify>
  <done>BATTERY_CATALOG has 8 entries with Sessy 5 at index 0 and enphase-5p present; every entry declares assumedFields per the map; assumedFields is optional on BatteryConfig; catalog tests + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render "geschat" badge per assumed spec row + "📄 Datasheet" link per card</name>
  <files>src/ui/battery-picker.ts, src/styles/battery-picker.css, tests/battery-picker.test.ts</files>
  <action>
In src/ui/battery-picker.ts, extend `appendSpec` to take an optional `isAssumed: boolean` param
(default false). When true, append a `<span class="battery-card__badge">geschat</span>` to the `dt`
via createElement + textContent (XSS-safe; static Dutch string — NO innerHTML). Add an
`aria-label` / `title` on the badge such as "geschatte waarde" for screen-reader/hover context.

In `buildSpecCard`, compute the assumed set once: `const assumed = new Set(battery.assumedFields ?? [])`.
Pass `assumed.has('nominalCapacityKwh')` to the Capaciteit row, `assumed.has('dodFraction')` to
Bruikbaar, `assumed.has('roundTripEfficiency')` to Rendement, `assumed.has('maxChargeKw')` to
Max laden, `assumed.has('maxDischargeKw')` to Max ontladen — using the label→field mapping in
<interfaces>. Custom cards (buildCustomCard) are user-entered and get NO badges — leave that
builder unchanged (assumedFields is undefined there ⇒ ?? [] ⇒ empty set).

Still in buildSpecCard, after the `<dl>` is appended, build a datasheet link:
create an `<a>`, then `setAttribute('href', battery.datasheetUrl)`,
`setAttribute('target', '_blank')`, `setAttribute('rel', 'noopener noreferrer')`,
className `battery-card__datasheet`, and `textContent = '📄 Datasheet'` (NO innerHTML — XSS/CSP
safe). Append it to the `li`. The link is a runtime user-navigation, not an auto-fetch — it does
not violate connect-src 'none' and does not appear in static index.html.

In src/styles/battery-picker.css, add `.battery-card__badge` (small inline pill using existing
design tokens — e.g. font-size var(--font-size-label), color var(--color-text-muted), a subtle
var(--color-surface) / var(--color-border) background+border, small padding/radius, inline-block,
left margin via var(--space-xs)) and `.battery-card__datasheet` (link styled with
var(--color-accent), font-size var(--font-size-label), inline-flex, gap, margin-top var(--space-sm),
plus a `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }` rule to
match the existing focus-ring convention). Use ONLY existing tokens — no inline styles, no new deps.

In tests/battery-picker.test.ts, add (jsdom) tests — keep all existing tests green:
(a) for a catalog battery WITH assumed fields (e.g. victron-ess-10), the rows for its assumed
    fields contain a `.battery-card__badge` with textContent 'geschat', and a cited row
    (e.g. its Capaciteit) does NOT;
(b) for sessy-5 (assumedFields []), the card renders ZERO `.battery-card__badge` elements;
(c) every catalog card has a `.battery-card__datasheet` anchor whose href equals the battery's
    datasheetUrl, with target="_blank" and rel containing 'noopener';
(d) keep the existing XSS test green and assert the picker subtree still has 0 `<script>` elements
    after rendering badges + links (textContent/setAttribute only). Confirm no element in the
    section carries an inline `style` attribute (the existing inline-style test must stay green).
  </action>
  <verify>
    <automated>TZ=Europe/Amsterdam npx vitest run tests/battery-picker.test.ts && npm run typecheck</automated>
  </verify>
  <done>geschat badge renders on every assumed spec row (and none on cited rows / custom cards); each catalog card has a working 📄 Datasheet link (href=datasheetUrl, target=_blank, rel=noopener noreferrer) built without innerHTML; badge + link styled via existing tokens with no inline styles; battery-picker tests + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full local CI + privacy-guard verification</name>
  <files></files>
  <action>
No code changes — this task is the gate. Run the complete local CI exactly as the project requires
and reproduce the deploy.yml privacy-guard grep against the freshly built dist/ to prove the 8th
catalog URL does not regress the guard.

If any step fails, return to Task 1 or 2 and fix; do not weaken the privacy guard, do not
mutate spec values away from the RESEARCH map, and do not switch the datasheet link to innerHTML.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run lint && npm run format:check && TZ=Europe/Amsterdam npm test && FOUND=$(grep -rEi --include='*.html' --include='*.js' --include='*.css' --include='*.json' --include='*.svg' --include='*.wasm' --include='*.webmanifest' '(https?:)?//[a-z0-9.-]|wss?://|\bfetch\(|XMLHttpRequest|navigator\.sendBeacon' dist/ | grep -v '\.map' | grep -v '//# sourceMappingURL' | sed -E 's#xmlns(:[a-zA-Z]+)?="https?://www\.w3\.org/[^"]*"##g' | grep -Ei '(https?:)?//[a-z0-9.-]|wss?://|\bfetch\(|XMLHttpRequest|navigator\.sendBeacon' || true); test -z "$FOUND" || { echo "PRIVACY GUARD FAILED:"; echo "$FOUND"; exit 1; }</automated>
  </verify>
  <done>typecheck, build, lint, format:check, and the full Vitest suite all pass under TZ=Europe/Amsterdam; the privacy-guard grep against the fresh dist/ reports nothing (PASS).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog data → DOM | Bundled spec strings (incl. datasheetUrl) are rendered into the picker UI |
| user → external site | The 📄 Datasheet link navigates the user's browser to a vendor domain (new tab) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w4s-01 | Tampering (XSS) | datasheet `<a>` + geschat badge in buildSpecCard | mitigate | Build via createElement + setAttribute(href/target/rel) + textContent only; NO innerHTML. Existing "0 <script> elements" and "no inline style" tests extended to cover the new nodes. |
| T-w4s-02 | Information disclosure | datasheet link / catalog URL in dist/ | mitigate | URLs live only in the bundled JS (dropped by the guard's `\.map` line-exclusion) and render at runtime as user-initiated navigations — never auto-fetched, never in static index.html. Final verify re-runs the deploy.yml privacy-guard grep against fresh dist/. |
| T-w4s-03 | Tampering (reverse tabnabbing) | `<a target="_blank">` | mitigate | `rel="noopener noreferrer"` set on the datasheet link so the opened vendor page cannot reach `window.opener`. |
| T-w4s-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added (NL-only, vanilla TS, no-new-deps constraint). No install step; package legitimacy gate not triggered. |
</threat_model>

<verification>
- `npm run typecheck` — assumedFields optional; no fixture/builder regressions.
- `npm run build` — production bundle builds clean.
- `npm run lint && npm run format:check` — style/lint clean.
- `TZ=Europe/Amsterdam npm test` — full Vitest suite green (catalog contract + picker DOM + XSS + inline-style + all prior tests).
- deploy.yml privacy-guard grep against fresh dist/ — PASS (no external URL / outbound-network reference leaks).
</verification>

<success_criteria>
- BATTERY_CATALOG ships 8 entries, Sessy 5 at index 0, enphase-5p present with exact RESEARCH specs.
- assumedFields is OPTIONAL on BatteryConfig and populated for all 8 entries per the RESEARCH map; custom batteries default to no assumed fields.
- A "geschat" badge renders next to every assumed spec row and on no cited row or custom card.
- Each catalog card has a "📄 Datasheet" link (href=datasheetUrl, target=_blank, rel=noopener noreferrer) built without innerHTML.
- Badge + link styled with existing design tokens; zero inline styles; no new dependencies.
- Full local CI green AND the privacy-guard grep passes against the fresh dist/.
</success_criteria>

<output>
Create `.planning/quick/260616-w4s-battery-catalog-provenance-ui-enphase-5p/260616-w4s-SUMMARY.md` when done
</output>
