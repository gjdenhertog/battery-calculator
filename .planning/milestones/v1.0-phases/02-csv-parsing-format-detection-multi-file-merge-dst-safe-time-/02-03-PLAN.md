---
phase: 02-csv-parsing-format-detection-multi-file-merge-dst-safe-time
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/domain/merge.ts
  - src/domain/gaps.ts
  - src/domain/period-filter.ts
  - tests/merge.test.ts
  - tests/period-filter.test.ts
autonomous: true
requirements: [DATA-05, DATA-10, DATA-11, DATA-12]

must_haves:
  truths:
    - "Merging two files where one is 15-min and one is hourly produces a series in which every overlapping timestamp comes from the finer-resolution file"
    - "The merge records, per file, how many samples it contributed and how many were overridden by a finer file"
    - "Gap detection infers the dominant cadence and counts missing expected intervals, treating the spring-forward missing hour and fall-back repeat hour as NOT gaps (DST-aware) (D-04)"
    - "Gaps are flag-and-counted only — IntervalSample[] contains only real samples, never zero-fill or synthetic intervals (D-05)"
    - "filterByPeriod narrows samples to [start,end] inclusive and defaults to the full range when bounds are null — pure function, no interactive date control this phase (D-02)"
    - "The merge result carries the gap count + gap ranges so the readout can surface them"
  artifacts:
    - path: "src/domain/merge.ts"
      provides: "mergeFiles() — finer-wins merge producing MergeResult with per-file stats + gap report"
      contains: "cadenceMinutes"
    - path: "src/domain/gaps.ts"
      provides: "detectGaps() — DST-aware cadence-based gap counting"
      contains: "export function detectGaps"
    - path: "src/domain/period-filter.ts"
      provides: "filterByPeriod() + fullRange()"
      contains: "export function filterByPeriod"
  key_links:
    - from: "src/domain/merge.ts"
      to: "src/domain/gaps.ts"
      via: "detectGaps call on merged samples"
      pattern: "detectGaps"
    - from: "src/domain/merge.ts"
      to: "src/domain/types.ts"
      via: "MergeResult / ParseFileResult import"
      pattern: "MergeResult"
---

<objective>
Build the pure post-parse domain functions: the finer-wins multi-file merge (with the
per-file contributed/overridden stats that make DATA-10 visible in the readout), DST-aware
gap detection, and the pure period filter. These are framework-free, node-runnable, and
have zero file overlap with Plan 02 — they run in parallel in Wave 2.

Purpose: DATA-10 (finer-wins merge), DATA-11 (gap count + per-file stats feeding the
readout), DATA-12 (pure tested period filter), and the DATA-05 monotonicity/first-interval
stats are surfaced through `MergeResult.fileStats`.

Output: `src/domain/merge.ts`, `src/domain/gaps.ts`, `src/domain/period-filter.ts`, and
two node-env test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md

<interfaces>
<!-- This plan CONSUMES the contract created in Plan 02-01. -->
From src/domain/types.ts:
  IntervalSample { timestamp: Date; gridImportKwh; gridExportKwh }
  ParseFileResult { fileName; encoding; seriesType; cadenceMinutes; samples; rowCount; isMonotonic; monotonicity_failRow?; firstIntervalAnomalyFlag; softWarnings? }
  MergeResult { samples: IntervalSample[]; gapCount; gapRanges: {from:Date;to:Date}[]; fileStats: FileStat[] }
  FileStat (per-file): fileName, encoding, seriesType, cadenceMinutes, rowCount, rowsContributed, rowsOverridden, isMonotonic, monotonicity_failRow?, firstIntervalAnomalyFlag, softWarnings
</interfaces>

<!-- Full sketches (do NOT inline in actions): -->
<!-- merge: 02-RESEARCH.md §"Pattern 5: Multi-File Finer-Wins Merge" -->
<!-- gaps: 02-RESEARCH.md §"Pattern 3: DST-Aware Gap Detection" (use the local-time walk per Open Question #3) -->
<!-- period: 02-RESEARCH.md §"Period Filter" -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Period filter (pure, tested)</name>
  <files>src/domain/period-filter.ts, tests/period-filter.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"Period Filter" (filterByPeriod + fullRange sketch)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md D-02 (pure function only, no UI control this phase)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/period-filter.ts" and §"Node-environment test files"
    - src/domain/types.ts
  </read_first>
  <behavior>
    - filterByPeriod(samples, start, end) returns only samples with start <= timestamp <= end (inclusive both ends).
    - filterByPeriod(samples, null, null) returns all samples (default full range — D-02).
    - filterByPeriod(samples, start, null) is open-ended on the right; (null, end) open-ended on the left.
    - fullRange(samples) returns { start: firstTimestamp, end: lastTimestamp }.
  </behavior>
  <action>
    Create `src/domain/period-filter.ts` exporting `filterByPeriod(samples: IntervalSample[], start: Date | null, end: Date | null): IntervalSample[]` (null bounds → ±Infinity, inclusive comparison on `timestamp.getTime()`) and `fullRange(samples): { start: Date; end: Date }`, per RESEARCH.md §Period Filter. Pure functions, no browser globals. Write `tests/period-filter.test.ts` (node env) covering the four behaviors with a small inline sample array (DATA-12, D-02).
  </action>
  <verify>
    <automated>npm test -- tests/period-filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/period-filter.test.ts` exits 0 (DATA-12).
    - Test asserts the null/null call returns an array of the same length as input (default full range).
    - Test asserts inclusive boundary behavior at both endpoints.
    - `grep -c "export function filterByPeriod" src/domain/period-filter.ts` returns 1.
  </acceptance_criteria>
  <done>filterByPeriod + fullRange implemented as pure functions; default-to-full-range and inclusive bounds proven; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DST-aware gap detection</name>
  <files>src/domain/gaps.ts, tests/merge.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"Pattern 3: DST-Aware Gap Detection", §"Pitfall 4" (spring-forward not a gap), and §"Open Questions" #3 (local-time walk recommended over tzScan)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md D-04, D-05 (gap = missing interval at cadence; DST-exempt; flag-and-count, never fabricate)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/gaps.ts"
    - src/domain/types.ts
  </read_first>
  <behavior>
    - A contiguous 15-min series has gapCount 0.
    - Removing one interior 15-min sample yields gapCount 1 with a gapRange covering the missing slot (D-04).
    - The spring-forward absent local hour (2026-03-29 02:00–02:59) is NOT counted as gaps (Pitfall 4, D-04).
    - The fall-back repeated local hour (2026-10-25 02:00–02:59 twice) is NOT counted as gaps and the extra slots are not treated as missing.
    - gaps.ts never inserts synthetic samples — it only counts/reports (D-05).
  </behavior>
  <action>
    Create `src/domain/gaps.ts` exporting `detectGaps(samples: IntervalSample[], cadenceMinutes: number): { count: number; ranges: { from: Date; to: Date }[] }` per RESEARCH.md §Pattern 3, using the local-time interval walk (Open Question #3 recommendation) so the spring-forward missing hour is naturally excluded and the fall-back extra hour is accepted — convert via `Europe/Amsterdam` (`@date-fns/tz`) when generating expected local slots, or detect DST transition blocks and exempt them. NEVER fabricate samples (D-05). Add the gap-detection assertions to `tests/merge.test.ts` (node env) — including the spring/fall DST-exemption cases (small inline series anchored on the DST dates). The merge-specific assertions land in Task 3 in the same file.
  </action>
  <verify>
    <automated>npm test -- tests/merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/merge.test.ts` exits 0 with the gap-detection cases passing (DATA-11, D-04).
    - Test asserts a deleted interior slot yields `count === 1` (D-04).
    - Test asserts the spring-forward 02:00–02:59 local block contributes 0 to the gap count (Pitfall 4).
    - `grep -c "export function detectGaps" src/domain/gaps.ts` returns 1.
  </acceptance_criteria>
  <done>detectGaps counts missing cadence slots, exempts DST spring/fall transitions, never fabricates data; gap cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finer-wins multi-file merge with per-file stats</name>
  <files>src/domain/merge.ts, tests/merge.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"Pattern 5: Multi-File Finer-Wins Merge" and §"IntervalSample Contract" (MergeResult.fileStats shape: rowsContributed/rowsOverridden)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md D-08 (per-file contributed-vs-overridden visibility), D-10 finer-wins
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/merge.ts"
    - src/domain/types.ts, src/domain/gaps.ts (created in Task 2)
  </read_first>
  <behavior>
    - mergeFiles([fine15min, hourly]) — for any timestamp present in both, the merged sample's values come from the 15-min file (DATA-10).
    - The hourly file's overlapping samples are counted as rowsOverridden; non-overlapping ones as rowsContributed (D-08).
    - The merged samples array is sorted ascending by timestamp.
    - The MergeResult carries gapCount/gapRanges from detectGaps over the merged series and copies the DATA-05 stats (isMonotonic, monotonicity_failRow, firstIntervalAnomalyFlag, softWarnings) into each fileStat.
  </behavior>
  <action>
    Create `src/domain/merge.ts` exporting `mergeFiles(results: ParseFileResult[]): MergeResult` per RESEARCH.md §Pattern 5: sort results finest-cadence-first, insert into a `Map<number, IntervalSample>` keyed by UTC ms skipping keys already present (finest wins), track per-file `rowsContributed`/`rowsOverridden`, sort the final samples ascending, infer the merged dominant cadence, call `detectGaps(mergedSamples, dominantCadence)` to populate `gapCount`/`gapRanges`, and build `fileStats` carrying each file's encoding/seriesType/cadenceMinutes/rowCount plus the DATA-05 monotonicity/first-interval/softWarnings fields. Add the finer-wins + per-file-stats assertions to `tests/merge.test.ts` (node env, alongside the Task 2 gap cases) using two inline ParseFileResult fixtures (one 15-min, one hourly, overlapping).
  </action>
  <verify>
    <automated>npm test -- tests/merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/merge.test.ts` exits 0 (DATA-10, DATA-11).
    - Test asserts an overlapping timestamp's merged values equal the 15-min file's values, not the hourly file's (DATA-10).
    - Test asserts the hourly file's overlapping rows are reflected in `rowsOverridden` (D-08).
    - Test asserts merged `samples` are sorted strictly ascending by timestamp.
    - Test asserts `MergeResult.gapCount` is populated from detectGaps (DATA-11).
  </acceptance_criteria>
  <done>mergeFiles produces a finer-wins merged series with per-file contributed/overridden stats and an embedded gap report; merge tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed samples → merge/gaps/period | Already-validated IntervalSample[] (non-negative, UTC) flows between pure domain functions; no new untrusted input enters here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | merge data integrity (wrong source wins) | mitigate | Finer-wins rule unit-tested (DATA-10); deterministic Map keyed by UTC ms. |
| T-02-08 | Tampering | gap fabrication / silent zero-fill | mitigate | gaps.ts only counts/reports; test asserts no synthetic samples are added (D-05). |
| T-02-09 | Information Disclosure | data exfiltration | accept | Pure functions, no `fetch`/XHR, no browser globals; `connect-src 'none'` from Plan 01 already blocks network. Low surface — no new boundary. |
</threat_model>

<verification>
- `npm test` full suite green (merge + period-filter).
- `npx tsc --noEmit` exits 0.
- merge.ts imports gaps.ts; no imports from src/ui/ (node-runnable domain layer).
</verification>

<success_criteria>
- DATA-10: finer-resolution file wins on overlapping timestamps (unit-asserted).
- DATA-11: gap count + per-file stats available for the readout.
- DATA-12: pure period filter defaults to full range and narrows inclusively.
- DATA-05: monotonicity/first-interval stats carried through fileStats.
</success_criteria>

<output>
Create `.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-03-SUMMARY.md` when done.
</output>
