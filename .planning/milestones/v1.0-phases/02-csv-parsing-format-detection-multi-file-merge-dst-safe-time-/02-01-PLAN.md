---
phase: 02-csv-parsing-format-detection-multi-file-merge-dst-safe-time
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/constants/csp.ts
  - tests/csp-plugin.test.ts
  - src/domain/types.ts
  - src/domain/parsers/registry.ts
  - tests/registry.test.ts
  - tests/fixtures/homewizard-spring-2026-03-29.csv
  - tests/fixtures/homewizard-fall-2026-10-25.csv
  - tests/fixtures/homewizard-real-sample.csv
autonomous: false
requirements: [DATA-03, DATA-06, DATA-07, DATA-08, DATA-09, DATA-13]
user_setup: []

must_haves:
  truths:
    - "All four locked runtime deps (papaparse, @types/papaparse, date-fns, @date-fns/tz) are in package.json at the locked versions and installed"
    - "The build CSP string contains worker-src blob: so PapaParse worker: true is not blocked"
    - "The domain type contract (IntervalSample, ParseFileResult, MergeResult, ParseRowError, UnsupportedEncodingError, SeriesType, CsvParser) exists and is importable in node"
    - "A parser registry exists where a second parser registers with zero edits to a central switch"
    - "DST fixture CSVs exist for 2026-03-29 (spring, 92 intervals expected) and 2026-10-25 (fall, 100 intervals expected)"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "IntervalSample contract + ParseFileResult + MergeResult + error classes + SeriesType"
      contains: "export interface IntervalSample"
    - path: "src/domain/parsers/registry.ts"
      provides: "CsvParser interface + ParserRegistry register/claim"
      contains: "export const ParserRegistry"
    - path: "src/constants/csp.ts"
      provides: "CSP string with worker-src blob:"
      contains: "worker-src blob:"
    - path: "tests/fixtures/homewizard-spring-2026-03-29.csv"
      provides: "Spring-forward DST fixture (23-hour local day)"
    - path: "tests/fixtures/homewizard-fall-2026-10-25.csv"
      provides: "Fall-back DST fixture (25-hour local day)"
  key_links:
    - from: "tests/csp-plugin.test.ts"
      to: "src/constants/csp.ts"
      via: "import { CSP }"
      pattern: "worker-src blob:"
    - from: "tests/registry.test.ts"
      to: "src/domain/parsers/registry.ts"
      via: "import { ParserRegistry }"
      pattern: "ParserRegistry\\.(register|claim)"
---

<objective>
Establish the Phase 2 foundation that every other plan depends on: install the locked
runtime stack (with a package-legitimacy checkpoint), relax the CSP for the PapaParse
blob worker (the [BLOCKING] change before any drop-zone worker integration), define the
canonical `src/domain/types.ts` contract, build the parser registry, and commit the DST
fixture CSVs (plus obtain the real HomeWizard sample).

Purpose: This plan is the shared interface and infrastructure layer. The `IntervalSample`
contract locked here is the input contract for Phase 3's `simulate()` and must be stable.
The CSP change unblocks DATA-13 worker verification. The registry is the seam for DATA-03.

Output: package.json runtime deps installed; `worker-src blob:` in CSP + its test;
`src/domain/types.ts`; `src/domain/parsers/registry.ts` + test; DST + real-sample fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md
@.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (02, 03, 04) import these. -->
<!-- Full type bodies + error-message strings are in 02-RESEARCH.md §IntervalSample Contract -->
<!-- and §Error Architecture; the registry shape is in §Pattern 1 / 02-PATTERNS.md. -->

src/domain/types.ts exports (final shape per RESEARCH.md):
  interface IntervalSample { timestamp: Date; gridImportKwh: number; gridExportKwh: number }
  type SeriesType = 'cumulative' | 'interval'
  interface ParseFileResult { fileName; encoding: 'UTF-8'|'Windows-1252'; seriesType; cadenceMinutes; samples: IntervalSample[]; rowCount; isMonotonic; monotonicity_failRow?; firstIntervalAnomalyFlag; softWarnings?: string[] }
  interface MergeResult { samples: IntervalSample[]; gapCount; gapRanges: {from:Date;to:Date}[]; fileStats: FileStat[] }
  class ParseRowError extends Error (fileName,rowNumber,columnName,expected,actual)
  class UnsupportedEncodingError extends Error (fileName)

src/domain/parsers/registry.ts exports:
  interface CsvParser { name: string; claim(headers: string[]): boolean; transform(rows: Record<string,string>[], file: File): ParseFileResult }
  const ParserRegistry = { register(p): void; claim(headers): CsvParser | null }
</interfaces>

Current CSP (src/constants/csp.ts) — see 02-PATTERNS.md lines 44-71 for verbatim array.
Current tests/csp-plugin.test.ts — one it() per directive, node env, see 02-PATTERNS.md lines 79-109.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify the four runtime deps before install</name>
  <read_first>
    - CLAUDE.md §"Recommended Stack" / §"Installation" (locked versions)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"Package Legitimacy Audit" (all four tagged [ASSUMED] — slopcheck unavailable at research time)
  </read_first>
  <action>Pause for human verification of package legitimacy before any install; do not run npm install until the reviewer approves.</action>
  <what-built>
    No code yet — this is the pre-install legitimacy gate required because RESEARCH.md tagged
    all four packages [ASSUMED] (slopcheck was unavailable). The packages are nonetheless the
    project's CLAUDE.md-locked authoritative stack.
  </what-built>
  <how-to-verify>
    Confirm each package on its registry page before install:
    1. https://www.npmjs.com/package/papaparse — repo github.com/mholt/PapaParse, ~10M weekly downloads, 5.5.3 published 2025-05-19.
    2. https://www.npmjs.com/package/@types/papaparse — DefinitelyTyped, tracks papaparse 5.x.
    3. https://www.npmjs.com/package/date-fns — repo github.com/date-fns/date-fns, ~30M+ weekly.
    4. https://www.npmjs.com/package/@date-fns/tz — official date-fns org companion, 1.5.0.
    Confirm none has a suspicious postinstall script.
  </how-to-verify>
  <acceptance_criteria>
    Reviewer types "approved" confirming all four packages match the RESEARCH.md audit table
    and CLAUDE.md locked versions. This checkpoint is never auto-approvable.
  </acceptance_criteria>
  <done>Reviewer has approved the four packages against the RESEARCH.md audit and CLAUDE.md locked versions.</done>
  <resume-signal>Type "approved" to proceed with install, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install locked runtime deps + relax CSP for the blob worker [BLOCKING]</name>
  <files>package.json, package-lock.json, src/constants/csp.ts, tests/csp-plugin.test.ts</files>
  <read_first>
    - CLAUDE.md §"Installation" and §"Version Compatibility" (locked version pins)
    - src/constants/csp.ts (the file being modified — current array)
    - tests/csp-plugin.test.ts (the file being modified — one it() per directive)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/constants/csp.ts" and §"tests/csp-plugin.test.ts" (exact change + pattern rule)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"CSP and PapaParse Worker — CRITICAL" (Option A rationale; security assessment)
  </read_first>
  <action>
    Install the four runtime deps with the CLAUDE.md-locked ranges: `npm install papaparse@^5.5.3 date-fns@^4.3.0 @date-fns/tz@^1.5.0` and `npm install --save-dev @types/papaparse@^5`. Do NOT change any existing devDependency version. Then implement RESEARCH.md Option A (recommended): in `src/constants/csp.ts`, insert the directive string `worker-src blob:` into the `CSP` array immediately between `"connect-src 'none'"` and `"base-uri 'self'"`, keeping the existing specificity ordering. Add a one-line comment on that directive citing DATA-13 + PapaParse `worker: true`, and update the file JSDoc to reference DATA-13. Do NOT add `worker-src 'self'`, do NOT touch `connect-src 'none'` (must stay `'none'` — exfiltration surface unchanged). In `tests/csp-plugin.test.ts`, add exactly one `it("contains worker-src blob:", ...)` block asserting `expect(CSP).toContain("worker-src blob:")`, matching the existing one-it()-per-directive structure; keep the no-`unsafe-inline` / no-`unsafe-eval` guards intact. Surface Option B (Vite `?worker` + `worker: false`) in the SUMMARY notes as the documented alternative if the owner later objects to the relaxation.
  </action>
  <verify>
    <automated>npm test -- tests/csp-plugin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains papaparse, date-fns, @date-fns/tz; `devDependencies` contains @types/papaparse — all at CLAUDE.md-locked ranges (DATA-13 install).
    - `grep -v '^[[:space:]]*\*' src/constants/csp.ts | grep -c "worker-src blob:"` returns 1 (DATA-13).
    - `grep -c "connect-src 'none'" src/constants/csp.ts` still returns 1 (lockdown preserved).
    - `npm test -- tests/csp-plugin.test.ts` exits 0 with the new `worker-src blob:` assertion green.
  </acceptance_criteria>
  <done>Runtime deps installed; CSP includes `worker-src blob:` with `connect-src 'none'` unchanged; CSP test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Domain type contract + parser registry</name>
  <files>src/domain/types.ts, src/domain/parsers/registry.ts, tests/registry.test.ts</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"IntervalSample Contract" and §"Error Architecture" (full type bodies + Dutch error-message strings)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-PATTERNS.md §"src/domain/types.ts", §"src/domain/parsers/registry.ts", and §"Node-environment test files" (greenfield patterns; named-export convention; node-env test header)
    - src/constants/csp.ts (project named-export convention reference)
  </read_first>
  <behavior>
    - registry.test.ts: registering two parsers and calling claim(headers) returns the first whose claim() matches (DATA-03).
    - registry.test.ts: a parser whose claim() returns false is never returned (proves the noop-stub pattern from criterion 7 works without a central switch).
    - registry.test.ts: claim() returns null when no parser matches.
  </behavior>
  <action>
    Create `src/domain/types.ts` with the exact exported shapes from RESEARCH.md §IntervalSample Contract: `IntervalSample` (UTC `Date` `timestamp`, non-negative `gridImportKwh`/`gridExportKwh` per DATA-06/DATA-07), `SeriesType`, `ParseFileResult` (include `rowsContributed`/`rowsOverridden` in the per-file stat shape that `MergeResult.fileStats` uses, and a `softWarnings: string[]` field for D-09), `MergeResult` with `gapCount`/`gapRanges`/`fileStats`, and the `ParseRowError` (fileName, rowNumber, columnName, expected, actual — DATA-09) and `UnsupportedEncodingError` (fileName) classes with the verbatim Dutch message strings from RESEARCH.md §Error Architecture. JSDoc every exported symbol with its invariant. Named exports only; no browser globals (`File` may appear only as a type). Create `src/domain/parsers/registry.ts` exporting the `CsvParser` interface and the `ParserRegistry` singleton object (`register`, `claim`) per RESEARCH.md §Pattern 1 — `claim` returns the first matching parser or null. Write `tests/registry.test.ts` (node env, no jsdom docblock) covering the behaviors above using inline throwaway parser objects.
  </action>
  <verify>
    <automated>npm test -- tests/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface IntervalSample" src/domain/types.ts` returns 1 (DATA-06/DATA-07).
    - `grep -c "export class ParseRowError" src/domain/types.ts` returns 1 (DATA-09).
    - `grep -c "export const ParserRegistry" src/domain/parsers/registry.ts` returns 1 (DATA-03).
    - `npm test -- tests/registry.test.ts` exits 0; the multi-parser / first-match / null-on-no-match assertions pass (DATA-03).
    - `npx tsc --noEmit` exits 0 (contract type-checks).
  </acceptance_criteria>
  <done>types.ts exports the locked contract; registry supports register/claim with first-match; registry test green; project type-checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: DST fixtures + obtain real HomeWizard P1 sample</name>
  <files>tests/fixtures/homewizard-spring-2026-03-29.csv, tests/fixtures/homewizard-fall-2026-10-25.csv, tests/fixtures/homewizard-real-sample.csv</files>
  <read_first>
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-RESEARCH.md §"HomeWizard P1 CSV Format" (column names, cumulative format, sample rows) and §"DST-Safe Bucketing with TZDate" (92/100 interval math) and §"Open Questions" #1 (real-sample gate)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-VALIDATION.md §"Wave 0 Requirements" (fixture list)
    - .planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-CONTEXT.md §canonical_refs (real-sample dependency on project owner)
  </read_first>
  <action>Synthesize the two DST fixture CSVs (spring 2026-03-29 and fall 2026-10-25) to the documented HomeWizard column spec, write them under tests/fixtures/, and pause for the reviewer to either supply a real HomeWizard sample or accept the synthesized fixtures.</action>
  <what-built>
    Two synthesized HomeWizard-format DST fixture CSVs are committed: a spring-forward day
    (2026-03-29, local 00:00 → next 00:00, with the absent 02:00–02:59 local hour skipped →
    23×4=92 interval rows after first-row delta reference) and a fall-back day (2026-10-25,
    both 02:00–02:59 local instances present → 25×4=100 rows). Both use the confirmed
    cumulative-meter column spec (`time,Import T1 kWh,Import T2 kWh,Export T1 kWh,Export T2 kWh`,
    comma delimiter, dot decimal, monotonically increasing values). The fixtures are
    synthesized from the documented spec so the suite is not blocked; the real sample is the
    open dependency.
  </what-built>
  <how-to-verify>
    1. The executor synthesizes the two DST fixtures matching the RESEARCH.md column spec, with
       row counts producing 92 and 100 IntervalSamples respectively (account for the first row
       being a delta reference — emit the correct raw row count so the post-delta sample count
       hits 92/100). Inspect the spring file: there must be NO row at local 02:00/02:15/02:30/02:45;
       the fall file must contain TWO blocks of 02:00–02:45 with distinct cumulative values.
    2. Provide a real HomeWizard P1 export at `tests/fixtures/homewizard-real-sample.csv` if you
       have one. If not available now, type "synth-only" and the synthesized fixtures stand in;
       the adapter is coded to the documented spec and a follow-up can swap in the real file.
  </how-to-verify>
  <acceptance_criteria>
    - `tests/fixtures/homewizard-spring-2026-03-29.csv` exists, header row is `time,Import T1 kWh,Import T2 kWh,Export T1 kWh,Export T2 kWh`, contains no `2026-03-29 02:` timestamp.
    - `tests/fixtures/homewizard-fall-2026-10-25.csv` exists and contains two `02:00`-hour blocks for 2026-10-25.
    - Reviewer types "approved" (real sample provided) or "synth-only" (synthesized fixtures accepted as stand-in). This gate is blocking because DATA-08 fixture validation in Plan 02 depends on these files.
  </acceptance_criteria>
  <done>Both DST fixtures exist under tests/fixtures/ and the reviewer has either supplied the real sample or accepted synth-only.</done>
  <resume-signal>Type "approved" (real sample committed) or "synth-only" (proceed with synthesized fixtures).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → shipped CSP | A CSP edit could silently widen the network/exfiltration surface |
| npm registry → installed deps | Untrusted package code enters the bundle at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `worker-src blob:` CSP relaxation | mitigate | Add only `worker-src blob:`; `connect-src 'none'` stays `'none'` (Task 2 acceptance asserts both). Blob worker is same-origin bundled PapaParse code — cannot fetch. |
| T-02-02 | Tampering | npm install of papaparse/date-fns/@date-fns/tz/@types/papaparse | mitigate | Task 1 blocking-human legitimacy gate verifies registry provenance + no postinstall scripts before install (T-02-SC). |
| T-02-SC | Tampering | npm installs ([ASSUMED] per RESEARCH.md) | mitigate | slopcheck + blocking human checkpoint (Task 1) for [ASSUMED] packages; never auto-approvable. |
</threat_model>

<verification>
- `npm test` full suite green (csp-plugin + registry tests).
- `npx tsc --noEmit` exits 0.
- CSP contains `worker-src blob:` and still `connect-src 'none'`.
- Four runtime deps present in package.json at locked versions.
- DST + real/synth fixtures present in tests/fixtures/.
</verification>

<success_criteria>
- DATA-13 (worker enablement): CSP no longer blocks the PapaParse blob worker.
- DATA-03 (registry seam): a second parser registers with zero central-switch edits.
- DATA-06/DATA-07/DATA-09 (contract): IntervalSample + ParseRowError defined and type-check.
- DATA-08 (fixtures staged): spring (92) and fall (100) DST fixtures committed.
</success_criteria>

<output>
Create `.planning/phases/02-csv-parsing-format-detection-multi-file-merge-dst-safe-time-/02-01-SUMMARY.md` when done.
</output>
