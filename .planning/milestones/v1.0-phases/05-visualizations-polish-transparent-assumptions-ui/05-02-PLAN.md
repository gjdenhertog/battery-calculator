---
phase: 05-visualizations-polish-transparent-assumptions-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/charts.css
  - src/styles/tooltips.css
  - src/styles/transparency-panel.css
  - src/styles/mobile-reflow.css
  - tests/terminology-audit.test.ts
autonomous: true
requirements: [UX-04, UX-05, UX-06]
must_haves:
  truths:
    - "A CI grep test fails the build if any banned terminology appears in src/ (UX-05)"
    - "A CI grep test fails the build if any email/contact/offerte CTA marker appears in src/ (UX-06)"
    - "Chart, tooltip, transparency-panel, and mobile-reflow styling is defined purely in CSS classes using existing tokens — no inline style"
    - "At <=480px the comparison table reflows to stacked per-battery cards and charts shrink within their containers (UX-04)"
  artifacts:
    - path: "tests/terminology-audit.test.ts"
      provides: "UX-05 + UX-06 contract-lock CI grep"
      contains: "findBanned"
    - path: "src/styles/charts.css"
      provides: "Chart section/wrapper/legend/caption/partial-label/sparse-note classes"
      contains: ".chart-wrapper"
    - path: "src/styles/tooltips.css"
      provides: "Dotted-underline term tooltip via ::after pseudo-element"
      contains: ".term-tooltip"
    - path: "src/styles/transparency-panel.css"
      provides: "Collapsible panel surface + summary touch-target styling"
      contains: ".transparency-panel"
    - path: "src/styles/mobile-reflow.css"
      provides: "@media (max-width: 480px) stacked-card + chart-height reflow"
      contains: "@media"
  key_links:
    - from: "tests/terminology-audit.test.ts"
      to: "src/ recursive file scan"
      via: "fs.readdirSync + readFileSync grep"
      pattern: "readFileSync"
    - from: "src/styles/mobile-reflow.css"
      to: "comparison-table data-label/data-metric hooks"
      via: "td::before content: attr(data-label)"
      pattern: "attr\\(data-label\\)"
---

<objective>
Land the four new Phase 5 CSS files and the two contract-locking CI grep audits (honest terminology + no-CTA).

Purpose: These artifacts have no file overlap with the chart/panel TypeScript, so they run in Wave 1 alongside the pure helpers. The CSS files define every visual class the chart adapters (Plan 03) and panel/tooltips (Plan 04) reference, so those plans never hand-roll styling. The grep audits follow the Phase 1 privacy-guard / Phase 2 DST-fixture contract-lock pattern: a failing grep fails the build, with no manual-pass escape hatch (D-12, D-14).
Output: charts.css, tooltips.css, transparency-panel.css, mobile-reflow.css, and tests/terminology-audit.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-CONTEXT.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md
@.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: terminology + no-CTA CI grep audit (UX-05, UX-06)</name>
  <files>tests/terminology-audit.test.ts</files>
  <read_first>
    - tests/csp-plugin.test.ts (the exact contract-lock test analog — node env, string-invariant assertions)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-RESEARCH.md (Pattern 8, Code Examples "Vitest CI Grep Test")
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Audit Terms UX-05/UX-06, CI Audit Tests)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (terminology-audit.test.ts section)
  </read_first>
  <action>Create tests/terminology-audit.test.ts in the default node env (no `// @vitest-environment` annotation). Implement a recursive allSrcFiles(dir) walk using readdirSync/statSync from fs collecting .ts and .html files under src/ (resolve src via join(import.meta.dirname, '..', 'src')), and a findBanned(terms) that opens each file with readFileSync(file, 'utf-8') and records `${file}: "${term}"` for any content.includes(term) hit. Write describe('UX-05 terminology audit') with one it asserting findBanned(['solar production', 'solar generation', 'zonne-opwekking', 'zonne-opbrengst']) has length 0. Write describe('UX-06 no-CTA audit') with one it asserting findBanned with the email/tel/offerte markers has length 0. The scan is scoped to src/ only (not tests/), so the banned literals living in this test file under tests/ are safe and will not self-invalidate. Per UX-05, `solar surplus` and the code field residualExportKwh are ALLOWED identifiers and must NOT be in the banned list. The failure message must list file+term so a future violation is actionable.</action>
  <verify>
    <automated>npm test -- terminology-audit --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- terminology-audit` passes against the current src/ (zero hits).
    - Banned-terminology list is exactly the four solar-drift terms; it does NOT contain "solar surplus", "teruglevering", or "residualExportKwh".
    - No-CTA list contains at minimum the email field, tel field, and "offerte" markers.
    - The scan resolves src from `import.meta.dirname, '..', 'src'` and does not scan tests/ (grep the test source to confirm).
    - Negative-control: temporarily adding a banned term to any src/ .ts file turns the test red; revert without committing.
  </acceptance_criteria>
  <done>Both audits green against current src/; a banned term anywhere in src/ would fail the build.</done>
</task>

<task type="auto">
  <name>Task 2: charts.css + tooltips.css</name>
  <files>src/styles/charts.css, src/styles/tooltips.css</files>
  <read_first>
    - src/styles/results-region.css (section/wrapper conventions; .results-section-heading already defined here — do NOT redefine; period-input min-height:44px touch target)
    - src/styles/comparison-table.css (.battery-swatch--N color slots; .results-stale dim pattern; .saldering-info-btn:focus-visible ring)
    - src/styles/tokens.css (all --space-*, --color-*, --font-size-*, --color-battery-N custom properties)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 1 and 4; "Technical-Term Tooltips" full CSS block; Color; Typography; New CSS Files)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (charts.css, tooltips.css sections)
  </read_first>
  <action>Create src/styles/charts.css defining (per UI-SPEC New CSS Files): .chart-section (margin-top var(--space-xl)), .chart-section__header (flex row, heading + dropdown label, gap var(--space-md)), .chart-wrapper (width 100%, height 280px, background var(--color-bg), 1px solid var(--color-border), border-radius 8px, overflow hidden), .chart-battery-select and .chart-battery-select__label (select gets min-height 44px touch target), .chart-week-caption (var(--font-size-label), var(--color-text-muted)), .chart-partial-label (the "(deels)" annotation, var(--color-text-muted), var(--font-size-label)), .chart-sparse-note (var(--color-surface) background, var(--space-sm) padding, border-radius 4px, var(--color-text-muted)), .chart-legend (flex wrap, gap var(--space-sm), var(--font-size-label), var(--color-text-muted)), .chart-legend__item (flex, align center, gap var(--space-xs)), .chart-legend__swatch, .chart-legend__label, and .chart-wrapper--computing (opacity 0.5; pointer-events none — mirrors .results-stale). Reuse .battery-swatch--N for legend swatches; do NOT redefine .results-section-heading. Create src/styles/tooltips.css copying the full .term-tooltip ruleset from UI-SPEC "Technical-Term Tooltips" verbatim: dotted underline + cursor help + position relative; .term-tooltip::after with content: attr(data-tooltip), absolute position, var(--color-text) bg / var(--color-bg) text, max-width 280px, z-index 10, opacity 0, transition none; :hover::after, :focus-visible::after, and .term-tooltip--open::after each set opacity 1. All values via existing tokens (border-radius 8px/4px literals match comparison-table.css). NO inline style, NO new color tokens, NO CSS transitions other than transition: none.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/styles/charts.css','utf8');const t=fs.readFileSync('src/styles/tooltips.css','utf8');for(const n of ['.chart-wrapper','.chart-legend','.chart-sparse-note','.chart-wrapper--computing']){if(!c.includes(n)){console.error('charts.css missing '+n);process.exit(1)}}for(const n of ['.term-tooltip','content: attr(data-tooltip)','.term-tooltip--open']){if(!t.includes(n)){console.error('tooltips.css missing '+n);process.exit(1)}}console.log('css ok')"</automated>
    <automated>node -e "const fs=require('fs');for(const f of ['src/styles/charts.css','src/styles/tooltips.css']){if(/style\s*=\s*\x22/.test(fs.readFileSync(f,'utf8'))){console.error('inline style in '+f);process.exit(1)}}console.log('no inline style')"</automated>
  </verify>
  <acceptance_criteria>
    - charts.css defines .chart-section, .chart-section__header, .chart-wrapper, .chart-battery-select, .chart-week-caption, .chart-partial-label, .chart-sparse-note, .chart-legend(+__item/__swatch/__label), .chart-wrapper--computing.
    - charts.css does NOT redefine .results-section-heading (grep absent).
    - tooltips.css contains `.term-tooltip`, `content: attr(data-tooltip)`, and the three opacity-1 selectors (:hover, :focus-visible, --open).
    - Neither file contains a literal `style=` attribute (CSP style-src 'self' hygiene).
    - The chart-battery select rule sets min-height: 44px.
  </acceptance_criteria>
  <done>charts.css and tooltips.css land with token-only, inline-style-free rules covering every class the chart adapters and tooltips need.</done>
</task>

<task type="auto">
  <name>Task 3: transparency-panel.css + mobile-reflow.css (UX-04)</name>
  <files>src/styles/transparency-panel.css, src/styles/mobile-reflow.css</files>
  <read_first>
    - src/styles/comparison-table.css (.saldering-disclaimer surface block: --color-surface bg, 1px border, border-radius 8px, padding --space-md; the data-metric column names like avoidedOff/avoidedOn; .battery-row / .battery-row__name classes)
    - src/styles/results-region.css (period-input min-height:44px touch-target pattern)
    - src/styles/global.css (the single existing @media (min-width:480px) block — mobile-reflow is its max-width inverse)
    - src/ui/comparison-table.ts lines 195-235 (td.dataset.metric / td.dataset.label hooks + .battery-row / .battery-row__name — the reflow targets)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-UI-SPEC.md (Component Inventory 3 styling; Component Inventory 5 "Mobile Reflow at 375px" full CSS block)
    - .planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-PATTERNS.md (transparency-panel.css, mobile-reflow.css sections)
  </read_first>
  <action>Create src/styles/transparency-panel.css per UI-SPEC Component Inventory 3: .transparency-panel (full-width block), .transparency-panel__details (var(--color-surface) bg, 1px solid var(--color-border), border-radius 8px, padding var(--space-md), margin-top var(--space-lg) — mirrors .saldering-disclaimer), .transparency-panel__summary (var(--font-size-heading), semibold, cursor pointer, min-height 44px WCAG touch target, list-style none, plus details[open] summary::before swapping the "▸ "/"▾ " Unicode glyph), .transparency-panel__summary:focus-visible (outline 2px solid var(--color-accent), offset 2px), .transparency-panel__body, .assumptions-list (var(--font-size-body), line-height 1.5, padding-left var(--space-lg)), .no-euros-section (margin-top var(--space-md), left border 4px solid var(--color-border), padding-left var(--space-md)), .no-euros-section__heading (var(--font-size-body), semibold), .no-euros-section__body (var(--font-size-body), var(--color-text-muted)). Create src/styles/mobile-reflow.css with a single `@media (max-width: 480px)` block copied from UI-SPEC Component Inventory 5: hide `.comparison-table thead`; set comparison-table/tbody/tr/td to display block width 100%; style `tr.battery-row` as a bordered card (1px border, border-radius 8px, margin-bottom var(--space-md), padding var(--space-md), var(--color-bg) bg); `td::before { content: attr(data-label); display block; var(--font-size-label); var(--color-text-muted) }`; style `td.battery-row__name` as the card heading (var(--font-size-heading), semibold, bottom border); shrink `#chart-monthly .chart-wrapper`/`.chart-wrapper` height to 220px and the flow `#chart-flow .chart-wrapper` to 260px with overflow-x: auto so the flow chart scrolls horizontally WITHIN its wrapper while headline numbers never scroll. Use the Phase 4 data-label/data-metric hooks (already emitted by comparison-table.ts). All values via tokens; NO inline style.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('src/styles/transparency-panel.css','utf8');const m=fs.readFileSync('src/styles/mobile-reflow.css','utf8');for(const n of ['.transparency-panel__details','.transparency-panel__summary','.assumptions-list','.no-euros-section']){if(!p.includes(n)){console.error('transparency-panel.css missing '+n);process.exit(1)}}if(!m.includes('@media (max-width: 480px)')){console.error('mobile-reflow missing media query');process.exit(1)}if(!m.includes('attr(data-label)')){console.error('mobile-reflow missing data-label hook');process.exit(1)}console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs');for(const f of ['src/styles/transparency-panel.css','src/styles/mobile-reflow.css']){if(/style\s*=\s*\x22/.test(fs.readFileSync(f,'utf8'))){console.error('inline style in '+f);process.exit(1)}}console.log('no inline style')"</automated>
  </verify>
  <acceptance_criteria>
    - transparency-panel.css defines .transparency-panel, .transparency-panel__details, .transparency-panel__summary (min-height 44px), :focus-visible outline using var(--color-accent), .transparency-panel__body, .assumptions-list, .no-euros-section(+__heading/__body).
    - mobile-reflow.css contains exactly one `@media (max-width: 480px)` block that hides .comparison-table thead, cards .battery-row, and uses content: attr(data-label) on td::before.
    - mobile-reflow.css shrinks chart wrapper heights (monthly 220px, flow 260px with overflow-x: auto).
    - Neither file contains a literal `style=` attribute.
  </acceptance_criteria>
  <done>Panel and mobile-reflow CSS land; the comparison table reflows to cards at <=480px using existing data-* hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author copy → CSS `content` | Static author-defined strings only; no user data reaches CSS `content` |
| (no runtime input boundary) | This plan ships only stylesheets and a build-time grep test; no user-string handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | inline style / CSP regression | mitigate | Both CSS tasks include an automated check that no `style=` literal exists; all styling is class-based with tokens, keeping style-src 'self' intact |
| T-05-03 | Information Disclosure | honesty/terminology drift | mitigate | tests/terminology-audit.test.ts (Task 1) fails the build on banned solar-production terms (UX-05) |
| T-05-04 | Spoofing | lead-capture / CTA injection | mitigate | Same grep audit fails the build on email/tel/offerte CTA markers (UX-06) |

No high-severity threats. CSS and a node-env grep test only; no network, no user-derived rendering.
</threat_model>

<verification>
- `npm test` full suite green (terminology-audit passes; existing suite unaffected).
- No `style=` literal in any of the four new CSS files.
- mobile-reflow uses the Phase 4 data-label hook.
</verification>

<success_criteria>
- UX-05 + UX-06 enforced by a failing-grep CI test.
- All four CSS files exist with token-only, inline-style-free rules covering every class Plan 03 and Plan 04 reference.
- UX-04 reflow rules in place for Plan 05's visual verification.
</success_criteria>

<output>
Create `.planning/phases/05-visualizations-polish-transparent-assumptions-ui/05-02-SUMMARY.md` when done.
</output>
