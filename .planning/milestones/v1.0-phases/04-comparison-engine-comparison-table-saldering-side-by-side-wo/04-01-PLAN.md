---
phase: 04-comparison-engine-comparison-table-saldering-side-by-side-wo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/constants/csp.ts
  - tests/csp-plugin.test.ts
  - src/workers/sim-worker.ts
  - tests/sim-worker-contract.test.ts
autonomous: true
requirements: [SIM-07]
user_setup: []

must_haves:
  truths:
    - "comlink and @preact/signals-core are installed and resolvable"
    - "Production vite build emits the Comlink sim-worker as a chunk under dist/assets/"
    - "CSP permits both the same-origin worker chunk ('self') and PapaParse's blob worker (blob:)"
    - "runComparison is importable directly from the worker entry module without instantiating a Worker"
  artifacts:
    - path: "src/workers/sim-worker.ts"
      provides: "Comlink worker entry exposing runComparison"
      max_lines: 12
    - path: "src/constants/csp.ts"
      provides: "Updated worker-src 'self' blob: directive"
      contains: "worker-src 'self' blob:"
    - path: "tests/sim-worker-contract.test.ts"
      provides: "Smoke test proving runComparison is importable from domain without a worker"
  key_links:
    - from: "src/workers/sim-worker.ts"
      to: "src/domain/compare.ts"
      via: "import { runComparison }"
      pattern: "import.*runComparison.*from.*domain/compare"
    - from: "src/constants/csp.ts"
      to: "vite build worker chunk + PapaParse blob worker"
      via: "worker-src directive"
      pattern: "worker-src 'self' blob:"
---

<objective>
Wave 0 gate: install the two new runtime dependencies (`comlink`, `@preact/signals-core`),
fix the CSP `worker-src` directive so the production Comlink worker chunk is permitted,
create the thin Comlink worker entry that re-exposes `runComparison`, and prove via a
production `vite build` that the worker chunk emits under `dist/assets/`. NO UI wave may
proceed until this plan is green — it is the phase's blocking gate (RESEARCH Open Q1, A3).

Purpose: De-risk the single HIGH-risk assumption of the phase (A3: Vite `?worker` emits a
same-origin `assets/` chunk that `worker-src blob:` alone would block). Establish the
pure-core / worker-shell split (SIM-07) where the same `runComparison` is consumed both by
Vitest (directly) and by the worker (via Comlink) — two consumers, one pure function.
Output: installed deps, corrected CSP, worker entry + contract test, verified build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-RESEARCH.md
@.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-PATTERNS.md

<interfaces>
<!-- The pure function the worker wraps. Already exists, do NOT modify. -->
From src/domain/compare.ts:
```typescript
export function runComparison(
  samples: IntervalSample[],
  batteries: BatteryConfig[],
  options?: SimOptions,
): SimResult[]
```

From src/constants/csp.ts (current line 22, the line to change):
```typescript
"worker-src blob:",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps and fix CSP worker-src directive</name>
  <files>package.json, package-lock.json, src/constants/csp.ts, tests/csp-plugin.test.ts</files>
  <read_first>
    - src/constants/csp.ts (the directive array; line 22 holds "worker-src blob:")
    - tests/csp-plugin.test.ts (the existing assertion that must be updated, ~line 39)
    - package.json (current dependencies block — comlink and @preact/signals-core absent)
    - .planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-RESEARCH.md §"Common Pitfalls" Pitfall 1
  </read_first>
  <action>
    Run `npm install comlink@^4.4.2 @preact/signals-core@^1.14.2` (both runtime deps, NOT devDeps) so they land in the `dependencies` block of package.json and update package-lock.json. These are CLAUDE.md-endorsed; both verified [ASSUMED]-legitimate in RESEARCH Package Legitimacy Audit (authoritative GoogleChromeLabs / preactjs source orgs, no postinstall) — no human-verify checkpoint required per research disposition.
    In src/constants/csp.ts, change the `"worker-src blob:",` array entry to `"worker-src 'self' blob:",` and update the adjacent comment block to state that BOTH workers are now whitelisted: the Vite `?worker` chunk emits at a same-origin `assets/xxx.js` URL (requires `'self'`) and PapaParse `worker: true` uses a blob URL (requires `blob:`). Keep `connect-src 'none'` unchanged — this is the privacy invariant; no network egress from any worker.
    In tests/csp-plugin.test.ts, update the existing `worker-src` assertion so it asserts the exact new directive `"worker-src 'self' blob:"` (change both the `it(...)` description and the `expect(CSP).toContain(...)` string). Do NOT add a second test — update the existing contract assertion in place.
  </action>
  <verify>
    <automated>node -e "require('comlink');require('@preact/signals-core')" && grep -q "worker-src 'self' blob:" src/constants/csp.ts && npm test -- --run tests/csp-plugin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains both `comlink` and `@preact/signals-core`
    - `src/constants/csp.ts` contains the exact substring `worker-src 'self' blob:`
    - `src/constants/csp.ts` still contains `connect-src 'none'` (privacy invariant unchanged)
    - `npm test -- --run tests/csp-plugin.test.ts` exits 0 and the worker-src assertion checks `"worker-src 'self' blob:"`
  </acceptance_criteria>
  <done>Both deps installed; CSP directive updated to `worker-src 'self' blob:`; csp-plugin test green against the new directive.</done>
</task>

<task type="auto">
  <name>Task 2: Create Comlink worker entry + contract smoke test + production build proof</name>
  <files>src/workers/sim-worker.ts, tests/sim-worker-contract.test.ts</files>
  <read_first>
    - src/domain/compare.ts (the runComparison export the worker re-exposes)
    - tests/compare.test.ts (node-env test pattern: describe/it, sample() fixture idiom)
    - .planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-PATTERNS.md §"src/workers/sim-worker.ts" and §"New Test Files"
    - .planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-RESEARCH.md §"Code Examples" (Comlink Worker File) and §"Open Questions" Q1
  </read_first>
  <action>
    Create `src/workers/sim-worker.ts` as a thin (≤12 line) Comlink adapter: import `* as Comlink from 'comlink'`, import `{ runComparison } from '../domain/compare'`, and call `Comlink.expose({ runComparison })`. Include the standard JSDoc file-header naming SIM-07, the pure-core/worker-shell split, and that tests never import this file. Do NOT add saldering, metrics, or any presentation logic here — it only bridges the worker boundary.
    Create `tests/sim-worker-contract.test.ts` (node env) that imports `runComparison` directly from `../src/domain/compare` (NEVER from the worker file, which references a `self`/Worker global unavailable in node) and asserts: it is a function, and calling it with an empty `batteries: []` returns an empty array (`expect(runComparison(samples, [])).toEqual([])`). This proves the dual-use contract: the pure function is independently testable without instantiating a Worker (SIM-07).
    Run a production `vite build` and confirm the Comlink worker is emitted as a separate JS chunk under `dist/assets/`. The build script is `npm run build` (runs `tsc -b` then `vite build`). Note: the worker is only bundled into a chunk once it is actually imported via the `?worker` suffix somewhere; since main-thread wiring lands in plan 04-03, for THIS plan verify the build succeeds with the worker entry present and typechecks clean. The full `assets/` worker-chunk emission is re-asserted at the phase build gate after 04-03 wires `new SimWorker()`. Document this sequencing in the SUMMARY.
  </action>
  <verify>
    <automated>npm test -- --run tests/sim-worker-contract.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/workers/sim-worker.ts` exists, is ≤12 lines, contains `Comlink.expose({ runComparison })`
    - `src/workers/sim-worker.ts` matches `import.*runComparison.*from '../domain/compare'`
    - `tests/sim-worker-contract.test.ts` imports `runComparison` from `../src/domain/compare` (NOT the worker file) and exits 0 via `npm test -- --run tests/sim-worker-contract.test.ts`
    - `npm run build` exits 0 (typecheck + vite build succeed with the new worker entry and updated CSP)
  </acceptance_criteria>
  <done>Worker entry created as a 5-line Comlink adapter; contract test proves runComparison is importable without a worker; production build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread → Comlink worker | inputs (samples, batteries) cross via structured-clone postMessage; no code crosses |
| build output → browser CSP | the emitted worker chunk URL must satisfy worker-src |
| npm registry → build | new package tarballs enter the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Comlink postMessage channel | mitigate | Comlink uses structured clone only — no function/code serialization; worker-src 'self' blob: limits worker origin to same-origin chunk + PapaParse blob, no remote worker URL |
| T-04-02 | Information Disclosure | worker network egress | mitigate | `connect-src 'none'` left unchanged; the worker inherits page CSP so no fetch/XHR is possible from inside it — user CSV data cannot leave the browser |
| T-04-03 | Elevation of Privilege | over-broad CSP relaxation | accept | `worker-src 'self' blob:` is the minimal set needed for the two real workers; `'self'` (assets chunk) + `blob:` (PapaParse) — no `data:`, no remote origins added |
| T-04-SC | Tampering | npm installs (comlink, @preact/signals-core) | mitigate | Both packages [ASSUMED]-legitimate per RESEARCH audit (authoritative source orgs, no postinstall, registry age 3-6 yrs); slopcheck unavailable but disposition is documented; install pinned to `^4.4.2` / `^1.14.2` |
</threat_model>

<verification>
- `node -e "require('comlink');require('@preact/signals-core')"` resolves both packages
- `grep "worker-src 'self' blob:" src/constants/csp.ts` matches
- `grep "connect-src 'none'" src/constants/csp.ts` still matches (privacy invariant)
- `npm test -- --run tests/csp-plugin.test.ts tests/sim-worker-contract.test.ts` exits 0
- `npm run build` exits 0
</verification>

<success_criteria>
- comlink and @preact/signals-core installed as runtime deps
- CSP updated to `worker-src 'self' blob:`, `connect-src 'none'` preserved
- sim-worker.ts is a ≤12-line Comlink adapter exposing runComparison
- sim-worker-contract.test.ts proves dual-use (pure fn importable without worker)
- production build green
</success_criteria>

<output>
Create `.planning/phases/04-comparison-engine-comparison-table-saldering-side-by-side-wo/04-01-SUMMARY.md` when done
</output>
