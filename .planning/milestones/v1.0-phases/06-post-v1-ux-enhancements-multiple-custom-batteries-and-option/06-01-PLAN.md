---
phase: 06-post-v1-ux-enhancements-multiple-custom-batteries-and-option
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/signals.ts
  - src/state/app-state.ts
  - tests/app-state.test.ts
autonomous: true
requirements: [BATT-05]
user_setup: []

must_haves:
  truths:
    - "customBatteries holds a collection of custom batteries (array), not a single nullable config (D-09)"
    - "salderingOn boolean signal exists and defaults to false (D-09, D-06)"
    - "activeBatteries appends only customs with nominalCapacityKwh > 0, in array order, after selectedBatteries (D-03, D-05)"
    - "An invalid/empty draft custom (nominalCapacityKwh <= 0 or missing) does NOT appear in activeBatteries and does NOT consume a cap slot (D-03)"
  artifacts:
    - path: "src/state/signals.ts"
      provides: "customBatteries collection signal + salderingOn boolean signal + array-aware activeBatteries computed"
      contains: "customBatteries"
    - path: "src/state/app-state.ts"
      provides: "re-export of customBatteries + salderingOn from the worker-free signal graph"
      contains: "salderingOn"
    - path: "tests/app-state.test.ts"
      provides: "node-env signal contract tests for the collection + saldering + activeBatteries filter"
      contains: "customBatteries"
  key_links:
    - from: "src/state/signals.ts activeBatteries"
      to: "customBatteries.value valid-filter"
      via: "computed spread [...selectedBatteries, ...validCustoms]"
      pattern: "customBatteries\\.value\\.filter"
    - from: "src/state/app-state.ts"
      to: "src/state/signals.ts"
      via: "re-export block"
      pattern: "salderingOn"
---

<objective>
Replace the single-custom data model with a custom-battery collection and add a saldering-on toggle signal — the worker-free state foundation both Wave 2 UI plans consume.

Purpose: Decisions D-09 (collection signal + unique-id customs + salderingOn boolean), D-03 (only valid customs count), and D-05 (order-based color reflow) all key off this state shape. The picker (06-02) and the table (06-03) read these signals; they cannot be built correctly until the signal contract is in place. This is a pure logic/state change with zero browser globals, fully testable in node Vitest (bypassing the worker-mock blind spot for the signal layer).

Output: Updated `src/state/signals.ts` (collection + boolean + array-aware computed), updated `src/state/app-state.ts` re-export list, and node-env contract tests in `tests/app-state.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-CONTEXT.md
@.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md

<interfaces>
<!-- Current signal contract the executor extends. Use these directly — no exploration. -->

From src/state/signals.ts (CURRENT — to be changed):
```typescript
export const customBattery = signal<Partial<BatteryConfig> | null>(null)
export const activeBatteries = computed(() => {
  const cb = customBattery.value
  const valid = cb !== null && (cb.nominalCapacityKwh ?? 0) > 0 ? [cb as BatteryConfig] : []
  return [...selectedBatteries.value, ...valid]
})
```

From src/domain/types.ts (consumed, NOT changed):
```typescript
export interface BatteryConfig {
  id: string; name: string;
  nominalCapacityKwh: number; dodFraction: number;
  roundTripEfficiency: number; maxChargeKw: number; maxDischargeKw: number;
  datasheetUrl?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace single-custom with customBatteries collection + add salderingOn signal + array-aware activeBatteries</name>
  <files>src/state/signals.ts, src/state/app-state.ts</files>
  <read_first>
    - src/state/signals.ts (full file — the module being changed; note the docblock lines 1-24, the `customBattery` declaration line 44, and the `activeBatteries` computed lines 90-94)
    - src/state/app-state.ts (lines 21-42 — the two re-export/import blocks; confirm `_runCompute` reads only `filteredSamples` + `activeBatteries`, so it needs NO change)
    - .planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md (§ "src/state/signals.ts" and § "src/state/app-state.ts" — exact target shapes with line refs)
    - src/domain/types.ts (BatteryConfig shape — the array element type)
  </read_first>
  <behavior>
    - customBatteries initial value is [] (empty array), typed signal<BatteryConfig[]>
    - salderingOn initial value is false, typed signal<boolean>
    - activeBatteries with no customs and default selection returns exactly [Sessy 5] (BATTERY_CATALOG[0])
    - activeBatteries with one custom of nominalCapacityKwh > 0 returns [...selected, thatCustom] in that order
    - activeBatteries with a custom of nominalCapacityKwh === 0 (or undefined) excludes it
    - activeBatteries preserves customBatteries array order among the valid customs (no sorting)
  </behavior>
  <action>
    In src/state/signals.ts: remove the `customBattery` declaration and replace it with `export const customBatteries = signal<BatteryConfig[]>([])` (empty-array initial, NOT null). Add `export const salderingOn = signal<boolean>(false)` directly after it (default false per D-06). Rewrite the `activeBatteries` computed to follow the PATTERNS spread shape: compute `validCustoms = customBatteries.value.filter((cb) => (cb.nominalCapacityKwh ?? 0) > 0)` then `return [...selectedBatteries.value, ...validCustoms]` — preserve array order, never sort (Pitfall 5). Update the module-level docblock (lines 1-24): in the Signal contract block replace the `customBattery` line with `customBatteries` and add a `salderingOn` line; in the Computed contract block update the `activeBatteries` description to "selectedBatteries + valid customBatteries (nominalCapacityKwh > 0) appended in order". Keep the `nominalCapacityKwh > 0` guard semantics identical to today (D-03).

    In src/state/app-state.ts: in the re-export block (lines 21-33) replace `customBattery` with `customBatteries` and add `salderingOn`. Do NOT change the internal import block for `_runCompute` (lines 36-42) — it does not reference the custom signal. No other logic changes; scheduleRecompute and the generation guard are untouched (D-10 worker pipeline unchanged).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | head; npm test -- app-state 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/state/signals.ts` contains `export const customBatteries = signal<BatteryConfig[]>([])` and `export const salderingOn = signal<boolean>(false)`
    - `src/state/signals.ts` no longer contains the identifier `customBattery` (singular) anywhere — verify: `grep -n "customBattery\b" src/state/signals.ts` returns nothing (the plural `customBatteries` is fine)
    - `src/state/app-state.ts` re-export block contains both `customBatteries` and `salderingOn`
    - `npx tsc --noEmit` reports zero errors originating from `src/state/signals.ts` or `src/state/app-state.ts` (downstream UI files still referencing the old name may error — those are fixed in 06-02/06-03 and are expected at this wave; note any such errors in the SUMMARY)
  </acceptance_criteria>
  <done>customBatteries collection + salderingOn signal exist and are re-exported; activeBatteries spreads valid customs in order; signals.ts has no singular customBattery reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Node-env contract tests for collection, saldering default, and valid-custom filter</name>
  <files>tests/app-state.test.ts</files>
  <read_first>
    - tests/app-state.test.ts (full file — existing node-env test structure; imports come from `../src/state/signals` NOT app-state, per worker-mock avoidance)
    - .planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-PATTERNS.md (§ "Test for signal contracts — node environment" lines 515-532; § "Fixture helpers in tests" lines 534-565)
    - src/state/signals.ts (the just-changed signal shapes)
  </read_first>
  <behavior>
    - Test: salderingOn defaults to false
    - Test: customBatteries defaults to [] and activeBatteries equals [BATTERY_CATALOG[0]] with default selection
    - Test: pushing one valid custom (nominalCapacityKwh: 6) via `customBatteries.value = [valid]` makes activeBatteries length 2 with the custom last
    - Test: a custom with nominalCapacityKwh: 0 is excluded from activeBatteries (length stays 1)
    - Test: two valid customs appear in activeBatteries in array order (custom-1 before custom-2)
  </behavior>
  <action>
    Add a new `describe('customBatteries + salderingOn signal contract')` block to tests/app-state.test.ts. Import `customBatteries`, `salderingOn`, `selectedBatteries`, `activeBatteries` from `../src/state/signals` (NOT app-state.ts — avoids the `?worker` suffix and the worker-mock path per the blind-spot note). Use a `makeBattery(overrides)` fixture matching the PATTERNS fixture shape with unique ids `custom-1`/`custom-2`. In `beforeEach`, reset `customBatteries.value = []`, `salderingOn.value = false`, `selectedBatteries.value = [BATTERY_CATALOG[0]]`. Write the five tests described in <behavior>. Assert array order explicitly with `.map((b) => b.id)` comparisons. These tests cover the signal layer only (node env) — the worker round-trip is out of scope here per the blind-spot note and is human-verified in 06-03.
  </action>
  <verify>
    <automated>npm test -- app-state 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app-state` exits 0
    - `tests/app-state.test.ts` contains the identifiers `customBatteries` and `salderingOn`
    - The new describe block has at least 5 `it(...)` cases covering: saldering default false, empty-collection activeBatteries, one valid custom appended, zero-capacity custom excluded, two-custom order preservation
    - Test imports resolve from `../src/state/signals` (assert: `grep -n "from '../src/state/signals'" tests/app-state.test.ts` matches the new imports)
  </acceptance_criteria>
  <done>Five node-env contract tests pass; collection + saldering default + valid-filter + order are locked by CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches only the pure signal graph. No user-controlled string reaches the DOM here; no network, no worker contract change. The custom-battery NAME field (the one untrusted-text surface for this phase) is handled in 06-02, not here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | activeBatteries valid-filter | mitigate | Keep the `nominalCapacityKwh > 0` guard (carried from the existing T-04-06 DoS guard) so a zero/negative-capacity draft can never enter the simulation input array. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in this plan. No package-manager install tasks; the existing RESEARCH Package Legitimacy Audit covers the already-installed deps. |

This plan introduces no new DOM-injection or network surface beyond existing patterns.
</threat_model>

<verification>
- `npx tsc --noEmit` shows no errors from signals.ts / app-state.ts.
- `npm test -- app-state` green.
- `grep -n "customBattery\b" src/state/signals.ts` returns nothing (singular name fully removed).
</verification>

<success_criteria>
- customBatteries collection + salderingOn boolean exist, re-exported via app-state.ts.
- activeBatteries appends valid customs in order; invalid drafts excluded.
- Node-env tests lock the contract; worker pipeline unchanged (D-10).
</success_criteria>

<output>
Create `.planning/phases/06-post-v1-ux-enhancements-multiple-custom-batteries-and-option/06-01-SUMMARY.md` when done. Note any downstream TS errors in battery-picker.ts / comparison-table.ts that 06-02/06-03 must resolve.
</output>
